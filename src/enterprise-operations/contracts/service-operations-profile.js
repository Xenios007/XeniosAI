export class ServiceOperationsProfile {
  constructor({
    serviceOperationsName,
    objectives = [],
    principles = [],
    lifecycleStages = [],
    classificationDimensions = [],
    serviceTypes = [],
    ownerAccountabilities = [],
    supportingRoles = [],
    catalogFields = [],
    commitmentTypes = [],
    commitmentQualityRequirements = [],
    serviceLevelIndicatorTypes = [],
    lifecycleStates = [],
    readinessRequirements = [],
    healthDimensions = [],
    healthStates = [],
    eventManagementSteps = [],
    eventCategories = [],
    requestTypes = [],
    requestRequirements = [],
    supportModelFields = [],
    supportTiers = [],
    dependencyTypes = [],
    dependencyOperationFields = [],
    sharedServiceControls = [],
    degradedModes = [],
    restorationPriorities = [],
    recoveryVerificationDimensions = [],
    maintenancePlanningFields = [],
    configurationRequirements = [],
    aiServiceHealthDimensions = [],
    workflowServiceHealthDimensions = [],
    integrationServiceHealthDimensions = [],
    dataServiceHealthDimensions = [],
    multiTenantScopes = [],
    propertyFacingContext = [],
    performanceReviewInputs = [],
    improvementSources = [],
    retirementRequirements = [],
    qualityAttributes = [],
    architecturalRules = [],
    futureCapabilities = [],
    businessOutcomeFirst = true,
    accountableServiceOwnerRequired = true,
    readinessBeforeProduction = true,
    healthMultidimensional = true,
    dependenciesOperatedExplicitly = true,
    failureBehaviorDefined = true,
    automationGoverned = true,
    continuousImprovementEnabled = true,
    catalogAuthoritative = true,
    commitmentsMeasurableOwnedReviewed = true,
    serviceHealthBusinessAligned = true,
    supportAndEscalationExplicit = true,
    degradedModesExplicitObservableCommunicatedTimeBounded = true,
    restorationEndToEndVerified = true,
    configurationAndMaintenanceGovernedAsChange = true,
    tenantPropertyIsolationPreserved = true,
    operationalObligationsCompleteBeforeRetirement = true,
    technologyNeutral = true,
    vendorNeutral = true,
    serviceManagementFrameworkSpecific = false,
    ticketingPlatformSpecific = false,
    monitoringProductSpecific = false,
    organizationalTeamSpecific = false,
    anonymousTechnicalComponent = false,
    technicalAvailabilityOnly = false,
    implementationSizeClassification = false,
    silentSecurityOrIsolationWeakening = false,
    providerAvailabilityEqualsAiQuality = false
  }) {
    this.serviceOperationsName = serviceOperationsName;
    this.objectives = Object.freeze([...objectives]);
    this.principles = Object.freeze([...principles]);
    this.lifecycleStages = Object.freeze([...lifecycleStages]);
    this.classificationDimensions = Object.freeze([...classificationDimensions]);
    this.serviceTypes = Object.freeze([...serviceTypes]);
    this.ownerAccountabilities = Object.freeze([...ownerAccountabilities]);
    this.supportingRoles = Object.freeze([...supportingRoles]);
    this.catalogFields = Object.freeze([...catalogFields]);
    this.commitmentTypes = Object.freeze([...commitmentTypes]);
    this.commitmentQualityRequirements = Object.freeze([...commitmentQualityRequirements]);
    this.serviceLevelIndicatorTypes = Object.freeze([...serviceLevelIndicatorTypes]);
    this.lifecycleStates = Object.freeze([...lifecycleStates]);
    this.readinessRequirements = Object.freeze([...readinessRequirements]);
    this.healthDimensions = Object.freeze([...healthDimensions]);
    this.healthStates = Object.freeze([...healthStates]);
    this.eventManagementSteps = Object.freeze([...eventManagementSteps]);
    this.eventCategories = Object.freeze([...eventCategories]);
    this.requestTypes = Object.freeze([...requestTypes]);
    this.requestRequirements = Object.freeze([...requestRequirements]);
    this.supportModelFields = Object.freeze([...supportModelFields]);
    this.supportTiers = Object.freeze([...supportTiers]);
    this.dependencyTypes = Object.freeze([...dependencyTypes]);
    this.dependencyOperationFields = Object.freeze([...dependencyOperationFields]);
    this.sharedServiceControls = Object.freeze([...sharedServiceControls]);
    this.degradedModes = Object.freeze([...degradedModes]);
    this.restorationPriorities = Object.freeze([...restorationPriorities]);
    this.recoveryVerificationDimensions = Object.freeze([...recoveryVerificationDimensions]);
    this.maintenancePlanningFields = Object.freeze([...maintenancePlanningFields]);
    this.configurationRequirements = Object.freeze([...configurationRequirements]);
    this.aiServiceHealthDimensions = Object.freeze([...aiServiceHealthDimensions]);
    this.workflowServiceHealthDimensions = Object.freeze([...workflowServiceHealthDimensions]);
    this.integrationServiceHealthDimensions = Object.freeze([...integrationServiceHealthDimensions]);
    this.dataServiceHealthDimensions = Object.freeze([...dataServiceHealthDimensions]);
    this.multiTenantScopes = Object.freeze([...multiTenantScopes]);
    this.propertyFacingContext = Object.freeze([...propertyFacingContext]);
    this.performanceReviewInputs = Object.freeze([...performanceReviewInputs]);
    this.improvementSources = Object.freeze([...improvementSources]);
    this.retirementRequirements = Object.freeze([...retirementRequirements]);
    this.qualityAttributes = Object.freeze([...qualityAttributes]);
    this.architecturalRules = Object.freeze([...architecturalRules]);
    this.futureCapabilities = Object.freeze([...futureCapabilities]);
    this.businessOutcomeFirst = Boolean(businessOutcomeFirst);
    this.accountableServiceOwnerRequired = Boolean(accountableServiceOwnerRequired);
    this.readinessBeforeProduction = Boolean(readinessBeforeProduction);
    this.healthMultidimensional = Boolean(healthMultidimensional);
    this.dependenciesOperatedExplicitly = Boolean(dependenciesOperatedExplicitly);
    this.failureBehaviorDefined = Boolean(failureBehaviorDefined);
    this.automationGoverned = Boolean(automationGoverned);
    this.continuousImprovementEnabled = Boolean(continuousImprovementEnabled);
    this.catalogAuthoritative = Boolean(catalogAuthoritative);
    this.commitmentsMeasurableOwnedReviewed = Boolean(commitmentsMeasurableOwnedReviewed);
    this.serviceHealthBusinessAligned = Boolean(serviceHealthBusinessAligned);
    this.supportAndEscalationExplicit = Boolean(supportAndEscalationExplicit);
    this.degradedModesExplicitObservableCommunicatedTimeBounded = Boolean(degradedModesExplicitObservableCommunicatedTimeBounded);
    this.restorationEndToEndVerified = Boolean(restorationEndToEndVerified);
    this.configurationAndMaintenanceGovernedAsChange = Boolean(configurationAndMaintenanceGovernedAsChange);
    this.tenantPropertyIsolationPreserved = Boolean(tenantPropertyIsolationPreserved);
    this.operationalObligationsCompleteBeforeRetirement = Boolean(operationalObligationsCompleteBeforeRetirement);
    this.technologyNeutral = Boolean(technologyNeutral);
    this.vendorNeutral = Boolean(vendorNeutral);
    this.serviceManagementFrameworkSpecific = Boolean(serviceManagementFrameworkSpecific);
    this.ticketingPlatformSpecific = Boolean(ticketingPlatformSpecific);
    this.monitoringProductSpecific = Boolean(monitoringProductSpecific);
    this.organizationalTeamSpecific = Boolean(organizationalTeamSpecific);
    this.anonymousTechnicalComponent = Boolean(anonymousTechnicalComponent);
    this.technicalAvailabilityOnly = Boolean(technicalAvailabilityOnly);
    this.implementationSizeClassification = Boolean(implementationSizeClassification);
    this.silentSecurityOrIsolationWeakening = Boolean(silentSecurityOrIsolationWeakening);
    this.providerAvailabilityEqualsAiQuality = Boolean(providerAvailabilityEqualsAiQuality);

    Object.freeze(this);
  }
}
