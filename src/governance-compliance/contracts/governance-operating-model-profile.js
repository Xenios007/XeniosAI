export class GovernanceOperatingModelProfile {
  constructor({
    modelName,
    objectives = [],
    principles = [],
    federationScopes = [],
    governanceLevels = [],
    authorities = [],
    roles = [],
    responsibilityTypes = [],
    decisionRightFields = [],
    decisionClasses = [],
    delegatedAuthorityRequirements = [],
    automatedAuthorityRequirements = [],
    forumTypes = [],
    forumCharterFields = [],
    assuranceLines = [],
    externalAssuranceProviders = [],
    decisionLifecycleStages = [],
    decisionRecordFields = [],
    escalationTriggers = [],
    emergencyGovernanceFields = [],
    governanceRhythms = [],
    informationTypes = [],
    reportViews = [],
    attestationFields = [],
    tenantGovernanceFields = [],
    propertyGovernanceFields = [],
    supplierGovernanceRoles = [],
    crossTenantGovernanceTriggers = [],
    governanceServiceTypes = [],
    effectivenessOutcomes = [],
    maturityStages = [],
    reviewTriggers = [],
    qualityAttributes = [],
    architecturalRules = [],
    futureCapabilities = [],
    federatedModel = true,
    decisionsNearAccountableScope = true,
    enterpriseGuardrailsPreserved = true,
    oneAccountableOwnerRequired = true,
    highRiskSeparationRequired = true,
    evidenceBeforeConclusion = true,
    committeeDoesNotOwnAccountability = true,
    delegatedAuthorityControlled = true,
    automatedAuthorityControlled = true,
    aiRecommendationNotApproval = true,
    assuranceIndependenceDefined = true,
    emergencyAuthorityTemporary = true,
    tenantSensitiveDetailsSegregated = true,
    supplierRiskInternallyOwned = true,
    outcomeBasedEffectiveness = true,
    vendorNeutral = true,
    technologyIndependent = true,
    prescribesOrgChart = false,
    prescribesBoardStructure = false,
    prescribesCommitteeNames = false,
    prescribesStaffingModel = false,
    prescribesVendorProduct = false
  }) {
    this.modelName = modelName;
    this.objectives = Object.freeze([...objectives]);
    this.principles = Object.freeze([...principles]);
    this.federationScopes = Object.freeze([...federationScopes]);
    this.governanceLevels = Object.freeze([...governanceLevels]);
    this.authorities = Object.freeze([...authorities]);
    this.roles = Object.freeze([...roles]);
    this.responsibilityTypes = Object.freeze([...responsibilityTypes]);
    this.decisionRightFields = Object.freeze([...decisionRightFields]);
    this.decisionClasses = Object.freeze([...decisionClasses]);
    this.delegatedAuthorityRequirements = Object.freeze([...delegatedAuthorityRequirements]);
    this.automatedAuthorityRequirements = Object.freeze([...automatedAuthorityRequirements]);
    this.forumTypes = Object.freeze([...forumTypes]);
    this.forumCharterFields = Object.freeze([...forumCharterFields]);
    this.assuranceLines = Object.freeze([...assuranceLines]);
    this.externalAssuranceProviders = Object.freeze([...externalAssuranceProviders]);
    this.decisionLifecycleStages = Object.freeze([...decisionLifecycleStages]);
    this.decisionRecordFields = Object.freeze([...decisionRecordFields]);
    this.escalationTriggers = Object.freeze([...escalationTriggers]);
    this.emergencyGovernanceFields = Object.freeze([...emergencyGovernanceFields]);
    this.governanceRhythms = Object.freeze([...governanceRhythms]);
    this.informationTypes = Object.freeze([...informationTypes]);
    this.reportViews = Object.freeze([...reportViews]);
    this.attestationFields = Object.freeze([...attestationFields]);
    this.tenantGovernanceFields = Object.freeze([...tenantGovernanceFields]);
    this.propertyGovernanceFields = Object.freeze([...propertyGovernanceFields]);
    this.supplierGovernanceRoles = Object.freeze([...supplierGovernanceRoles]);
    this.crossTenantGovernanceTriggers = Object.freeze([...crossTenantGovernanceTriggers]);
    this.governanceServiceTypes = Object.freeze([...governanceServiceTypes]);
    this.effectivenessOutcomes = Object.freeze([...effectivenessOutcomes]);
    this.maturityStages = Object.freeze([...maturityStages]);
    this.reviewTriggers = Object.freeze([...reviewTriggers]);
    this.qualityAttributes = Object.freeze([...qualityAttributes]);
    this.architecturalRules = Object.freeze([...architecturalRules]);
    this.futureCapabilities = Object.freeze([...futureCapabilities]);
    this.federatedModel = Boolean(federatedModel);
    this.decisionsNearAccountableScope = Boolean(decisionsNearAccountableScope);
    this.enterpriseGuardrailsPreserved = Boolean(enterpriseGuardrailsPreserved);
    this.oneAccountableOwnerRequired = Boolean(oneAccountableOwnerRequired);
    this.highRiskSeparationRequired = Boolean(highRiskSeparationRequired);
    this.evidenceBeforeConclusion = Boolean(evidenceBeforeConclusion);
    this.committeeDoesNotOwnAccountability = Boolean(committeeDoesNotOwnAccountability);
    this.delegatedAuthorityControlled = Boolean(delegatedAuthorityControlled);
    this.automatedAuthorityControlled = Boolean(automatedAuthorityControlled);
    this.aiRecommendationNotApproval = Boolean(aiRecommendationNotApproval);
    this.assuranceIndependenceDefined = Boolean(assuranceIndependenceDefined);
    this.emergencyAuthorityTemporary = Boolean(emergencyAuthorityTemporary);
    this.tenantSensitiveDetailsSegregated = Boolean(tenantSensitiveDetailsSegregated);
    this.supplierRiskInternallyOwned = Boolean(supplierRiskInternallyOwned);
    this.outcomeBasedEffectiveness = Boolean(outcomeBasedEffectiveness);
    this.vendorNeutral = Boolean(vendorNeutral);
    this.technologyIndependent = Boolean(technologyIndependent);
    this.prescribesOrgChart = Boolean(prescribesOrgChart);
    this.prescribesBoardStructure = Boolean(prescribesBoardStructure);
    this.prescribesCommitteeNames = Boolean(prescribesCommitteeNames);
    this.prescribesStaffingModel = Boolean(prescribesStaffingModel);
    this.prescribesVendorProduct = Boolean(prescribesVendorProduct);

    Object.freeze(this);
  }
}
