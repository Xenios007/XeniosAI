import { PlatformError } from '../../foundation/errors/platform-error.js'; import { MetricsMeasuresSemanticLayerProfile as Profile } from '../contracts/metrics-measures-semantic-layer-profile.js'; import * as constants from '../metrics-measures-semantic-layer-constants.js';
const metadata = Object.freeze({ responsibilities: constants.SEMANTIC_RESPONSIBILITIES, businessTermFields: constants.BUSINESS_TERM_FIELDS, dimensionFields: constants.DIMENSION_FIELDS, measureFields: constants.MEASURE_FIELDS, timeSemanticFields: constants.TIME_SEMANTIC_FIELDS, grainFields: constants.GRAIN_FIELDS, metricDefinitionFields: constants.METRIC_DEFINITION_FIELDS, metricResultFields: constants.METRIC_RESULT_FIELDS, controls: constants.SEMANTIC_CONTROLS, failureRecovery: constants.SEMANTIC_FAILURE_RECOVERY, observabilityFields: constants.SEMANTIC_OBSERVABILITY, assuranceEvidence: constants.SEMANTIC_ASSURANCE, invariants: constants.SEMANTIC_INVARIANTS });
const required = Object.freeze({ semanticOwnershipAccountable: 'requires accountable semantic ownership', definitionsVersioned: 'requires versioned definitions', grainExplicit: 'requires explicit grain', timeSemanticsExplicit: 'requires explicit time semantics', measuresDeterministic: 'requires deterministic measures', unitsValidated: 'requires unit and currency validation', lineageRequired: 'requires lineage', resultsQualified: 'requires qualified metric results', factsRemainExternal: 'requires operational facts to remain external' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', hiddenMetricFilter: 'prohibits hidden metric filters', implicitGrain: 'prohibits implicit grain', ambiguousTimeSemantic: 'prohibits ambiguous time semantics', mutablePublishedDefinition: 'prohibits mutable published definitions', telemetryAsBusinessFact: 'prohibits telemetry as business fact', inferredResultAsSourceTruth: 'prohibits inferred results as source truth' });
export class MetricsMeasuresSemanticLayerDescriptor {
  responsibilities() { return values(metadata.responsibilities); } businessTermFields() { return values(metadata.businessTermFields); } dimensionFields() { return values(metadata.dimensionFields); } measureFields() { return values(metadata.measureFields); } timeSemanticFields() { return values(metadata.timeSemanticFields); } grainFields() { return values(metadata.grainFields); } metricDefinitionFields() { return values(metadata.metricDefinitionFields); } metricResultFields() { return values(metadata.metricResultFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Metrics Measures Semantic Layer profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-028-02 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-028-02 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.METRICS_MEASURES_SEMANTIC_LAYER_ERROR_CODE, 'Metrics Measures Semantic Layer violates ARCH-028-02.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
