import { PlatformError } from '../../foundation/errors/platform-error.js'; import { AnalyticsDecisionIntelligenceOverviewProfile as Profile } from '../contracts/analytics-decision-intelligence-overview-profile.js'; import * as constants from '../analytics-decision-intelligence-overview-constants.js';
const metadata = Object.freeze({ responsibilities: constants.ANALYTICS_RESPONSIBILITIES, authorities: constants.ANALYTICS_AUTHORITIES, capabilities: constants.ANALYTICS_CAPABILITIES, contractFields: constants.ANALYTICS_CONTRACT_FIELDS, recordFields: constants.ANALYTICS_RECORD_FIELDS, lifecycleStates: constants.ANALYTICS_LIFECYCLE_STATES, governanceControls: constants.ANALYTICS_GOVERNANCE_CONTROLS, failureRecovery: constants.ANALYTICS_FAILURE_RECOVERY, observabilityFields: constants.ANALYTICS_OBSERVABILITY_FIELDS, assuranceEvidence: constants.ANALYTICS_ASSURANCE_EVIDENCE, invariants: constants.ANALYTICS_INVARIANTS });
const required = Object.freeze({ semanticDefinitionsGoverned: 'requires governed semantic definitions', analyticalProductsGoverned: 'requires governed analytical products', operationalFactsExternal: 'requires external operational-fact authority', telemetryAuthorityExternal: 'requires external telemetry authority', customerTruthExternal: 'requires external customer-truth authority', aiEvaluationExternal: 'requires ARCH-029 evaluation authority', derivedOutputsLabeled: 'requires labeled derived outputs', lineageRequired: 'requires lineage', tenantIsolation: 'requires tenant isolation', humanReviewAvailable: 'requires human review and appeal' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', telemetryBecomesBusinessTruth: 'prohibits telemetry becoming business truth', inferenceBecomesSourceTruth: 'prohibits inference becoming source truth', analyticsOwnsOperationalFacts: 'prohibits analytics ownership of operational facts', analyticsOwnsCustomerTruth: 'prohibits analytics ownership of customer truth', analyticalOutputExecutesConsequentialAction: 'prohibits analytical output directly executing consequential actions', aiPromotionOutsideArch029: 'prohibits AI promotion outside ARCH-029' });
export class AnalyticsDecisionIntelligenceOverviewDescriptor {
  responsibilities() { return values(metadata.responsibilities); } authorities() { return values(metadata.authorities); } capabilities() { return values(metadata.capabilities); } contractFields() { return values(metadata.contractFields); } recordFields() { return values(metadata.recordFields); } lifecycleStates() { return values(metadata.lifecycleStates); } governanceControls() { return values(metadata.governanceControls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Analytics Decision Intelligence Overview profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-028-01 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-028-01 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.ANALYTICS_DECISION_INTELLIGENCE_OVERVIEW_ERROR_CODE, 'Analytics Decision Intelligence Overview violates ARCH-028-01.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
