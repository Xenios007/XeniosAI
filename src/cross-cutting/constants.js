export const HEALTH_STATUS = Object.freeze({
  HEALTHY: 'healthy',
  DEGRADED: 'degraded',
  UNHEALTHY: 'unhealthy'
});

export const AUDIT_OUTCOMES = Object.freeze({
  SUCCESS: 'success',
  FAILURE: 'failure',
  DENIED: 'denied'
});

export const CROSS_CUTTING_ERROR_CODES = Object.freeze({
  VALIDATION_FAILED: 'CROSS_CUTTING_VALIDATION_FAILED'
});
