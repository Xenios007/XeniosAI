import { PlatformError } from '../../foundation/errors/platform-error.js';
import { DocumentationSupportFeedbackCommunityProfile as Profile } from '../contracts/documentation-support-feedback-community-profile.js';
import * as constants from '../documentation-support-feedback-community-constants.js';

const metadata = Object.freeze({ documentationFields: constants.DOCUMENTATION_FIELDS, exampleFields: constants.EXAMPLE_FIELDS, supportCaseFields: constants.SUPPORT_CASE_FIELDS, feedbackFields: constants.FEEDBACK_FIELDS, announcementFields: constants.ANNOUNCEMENT_FIELDS, learningCommunityFields: constants.LEARNING_COMMUNITY_FIELDS, controls: constants.DOC_SUPPORT_CONTROLS, failureRecovery: constants.DOC_SUPPORT_RECOVERY, observabilityFields: constants.DOC_SUPPORT_OBSERVABILITY, assuranceEvidence: constants.DOC_SUPPORT_ASSURANCE, invariants: constants.DOC_SUPPORT_INVARIANTS });
const required = Object.freeze({ documentationVersionMatched: 'requires version-matched documentation', examplesSecretFree: 'requires secret-free examples', supportAudited: 'requires audited support', feedbackGoverned: 'requires governed feedback', announcementsApproved: 'requires approved announcements', learningAccessible: 'requires accessible learning', communityLinksVetted: 'requires vetted community links', providerAuthorityPreserved: 'requires preserved provider authority' });
const prohibited = Object.freeze({ documentationGrantsAuthority: 'prohibits documentation granting authority', examplesContainCredentials: 'prohibits credentials in examples', supportBypassesProviders: 'prohibits support bypassing providers', feedbackBecomesTruth: 'prohibits feedback becoming authoritative truth', feedbackDirectlyChangesProduction: 'prohibits feedback directly changing production', announcementUnapproved: 'prohibits unapproved announcements', unsafeCommunityLink: 'prohibits unsafe community links', staleContentUnlabeled: 'prohibits unlabeled stale content', machineSelfPublishes: 'prohibits machine self-publication', protectedPayloadInTelemetry: 'prohibits protected payloads in telemetry' });

export class DocumentationSupportFeedbackCommunityDescriptor {
  documentationFields() { return values(metadata.documentationFields); } exampleFields() { return values(metadata.exampleFields); } supportCaseFields() { return values(metadata.supportCaseFields); } feedbackFields() { return values(metadata.feedbackFields); } announcementFields() { return values(metadata.announcementFields); } learningCommunityFields() { return values(metadata.learningCommunityFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Documentation Support Feedback and Community profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-031-07 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-031-07 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.DOCUMENTATION_SUPPORT_FEEDBACK_COMMUNITY_ERROR_CODE, 'Documentation Support Feedback and Community violates ARCH-031-07.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
