import { PlatformError } from '../../foundation/errors/platform-error.js';
import { DeveloperPersonasJourneysOnboardingProfile as Profile } from '../contracts/developer-personas-journeys-onboarding-profile.js';
import * as constants from '../developer-personas-journeys-onboarding-constants.js';
const metadata = Object.freeze({ personaFields: constants.DEVELOPER_PERSONA_FIELDS, prerequisiteFields: constants.ONBOARDING_PREREQUISITE_FIELDS, verificationFields: constants.DEVELOPER_VERIFICATION_FIELDS, agreementFields: constants.DEVELOPER_AGREEMENT_FIELDS, onboardingFields: constants.GUIDED_ONBOARDING_FIELDS, lifecycleStates: constants.DEVELOPER_ONBOARDING_LIFECYCLE, controls: constants.DEVELOPER_ONBOARDING_CONTROLS, failureRecovery: constants.DEVELOPER_ONBOARDING_FAILURE_RECOVERY, measures: constants.DEVELOPER_ONBOARDING_MEASURES, observabilityFields: constants.DEVELOPER_ONBOARDING_OBSERVABILITY, assuranceEvidence: constants.DEVELOPER_ONBOARDING_ASSURANCE, invariants: constants.DEVELOPER_ONBOARDING_INVARIANTS });
const required = Object.freeze({ personasPurposeScoped: 'requires purpose-scoped personas', prerequisitesVerified: 'requires verified prerequisites', agreementsExplicit: 'requires explicit versioned agreements', onboardingAccessible: 'requires accessible onboarding', providerAuthorizationPreserved: 'requires provider authorization', firstUseProviderConfirmed: 'requires provider-confirmed first use', correctionAppealAvailable: 'requires correction and appeal', tenantIsolated: 'requires tenant isolation' });
const prohibited = Object.freeze({ identityAssumedFromPersona: 'prohibits identity being assumed from persona selection', organizationMembershipUnverified: 'prohibits unverified organization membership', agreementsSkipped: 'prohibits skipped required agreements', portalAutoApprovesAccess: 'prohibits portal automatic access approval', credentialsEmbeddedInGuidance: 'prohibits credentials embedded in guidance', inaccessibleJourney: 'prohibits inaccessible onboarding journeys', interfaceAcknowledgementMeansSuccess: 'prohibits interface acknowledgement from meaning onboarding success', crossTenantOnboardingReuse: 'prohibits cross-tenant onboarding reuse', machineExpandsPermissions: 'prohibits machine-expanded permissions', protectedPayloadInTelemetry: 'prohibits protected payloads in telemetry' });
export class DeveloperPersonasJourneysOnboardingDescriptor {
  personaFields() { return values(metadata.personaFields); } prerequisiteFields() { return values(metadata.prerequisiteFields); } verificationFields() { return values(metadata.verificationFields); } agreementFields() { return values(metadata.agreementFields); } onboardingFields() { return values(metadata.onboardingFields); } lifecycleStates() { return values(metadata.lifecycleStates); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } measures() { return values(metadata.measures); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Developer Personas Journeys and Onboarding profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-031-02 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-031-02 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.DEVELOPER_PERSONAS_JOURNEYS_ONBOARDING_ERROR_CODE, 'Developer Personas Journeys and Onboarding violates ARCH-031-02.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
