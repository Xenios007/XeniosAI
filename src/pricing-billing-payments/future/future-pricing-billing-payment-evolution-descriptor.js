import { PlatformError } from '../../foundation/errors/platform-error.js';
import { FuturePricingBillingPaymentEvolutionProfile as Profile } from '../contracts/future-pricing-billing-payment-evolution-profile.js';
import * as constants from '../future-pricing-billing-payment-evolution-constants.js';
const metadata = Object.freeze({ evolutionPrinciples: constants.FINANCIAL_EVOLUTION_PRINCIPLES, evolutionHorizons: constants.FINANCIAL_EVOLUTION_HORIZONS, realTimeFinance: constants.REAL_TIME_FINANCE, usageBasedFinance: constants.USAGE_BASED_FINANCE, multiPartyFinance: constants.MULTI_PARTY_FINANCE, intelligentFinance: constants.INTELLIGENT_FINANCE, researchPromotionRetirement: constants.FINANCIAL_RESEARCH_PROMOTION_RETIREMENT, controls: constants.FINANCIAL_EVOLUTION_CONTROLS, measures: constants.FINANCIAL_EVOLUTION_MEASURES, invariants: constants.FINANCIAL_EVOLUTION_INVARIANTS });
const required = Object.freeze({ financialAuthorityEndures: 'requires enduring financial authority', calculationsRemainDeterministic: 'requires deterministic calculations', humanApprovalRequired: 'requires human approval', boundedExperimentationRequired: 'requires bounded experimentation', rollbackRequired: 'requires rollback', portableSemanticsRequired: 'requires portable semantics', evidenceRequired: 'requires evidence', aiChangesOnlyThroughArch029: 'requires AI promotion through ARCH-029', aiCannotChangeMoney: 'prohibits AI financial authority', tenantBoundariesExplicit: 'requires explicit tenant boundaries' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', unboundedExperimentation: 'prohibits unbounded experimentation', aiChangesMoney: 'prohibits AI from changing money', aiOwnsFinancialState: 'prohibits AI from owning financial state', promotionOutsideArch029: 'prohibits AI promotion outside ARCH-029', unreconciledMultiPartySettlement: 'prohibits unreconciled multi-party settlement' });
export class FuturePricingBillingPaymentEvolutionDescriptor {
  evolutionPrinciples() { return values(metadata.evolutionPrinciples); } evolutionHorizons() { return values(metadata.evolutionHorizons); } realTimeFinance() { return values(metadata.realTimeFinance); } usageBasedFinance() { return values(metadata.usageBasedFinance); } multiPartyFinance() { return values(metadata.multiPartyFinance); } intelligentFinance() { return values(metadata.intelligentFinance); } researchPromotionRetirement() { return values(metadata.researchPromotionRetirement); } controls() { return values(metadata.controls); } measures() { return values(metadata.measures); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Future Pricing Billing Payment Evolution profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-026-10 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-026-10 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.FUTURE_PRICING_BILLING_PAYMENT_EVOLUTION_ERROR_CODE, 'Future Pricing Billing Payment Evolution violates ARCH-026-10.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
