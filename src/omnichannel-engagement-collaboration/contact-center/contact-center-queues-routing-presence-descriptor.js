import { PlatformError } from '../../foundation/errors/platform-error.js'; import { ContactCenterQueuesRoutingPresenceProfile as Profile } from '../contracts/contact-center-queues-routing-presence-profile.js'; import * as constants from '../contact-center-queues-routing-presence-constants.js';
const metadata = Object.freeze({ responsibilities: constants.CONTACT_CENTER_RESPONSIBILITIES, queueFields: constants.QUEUE_FIELDS, skillFields: constants.SKILL_FIELDS, priorityFields: constants.PRIORITY_FIELDS, assignmentFields: constants.ASSIGNMENT_FIELDS, presenceFields: constants.PRESENCE_FIELDS, capacityFields: constants.CAPACITY_FIELDS, routingDecisionFields: constants.ROUTING_DECISION_FIELDS, escalationFields: constants.ESCALATION_FIELDS, controls: constants.ROUTING_CONTROLS, failureRecovery: constants.ROUTING_FAILURE_RECOVERY, observabilityFields: constants.ROUTING_OBSERVABILITY_FIELDS, assuranceEvidence: constants.ROUTING_ASSURANCE_EVIDENCE, invariants: constants.ROUTING_INVARIANTS });
const required = Object.freeze({ queuesTenantScoped: 'requires tenant-scoped queues', skillsVerified: 'requires verified skills', prioritiesBounded: 'requires bounded priority', presenceFreshnessExplicit: 'requires explicit presence freshness', capacityEnforced: 'requires capacity enforcement', routingDeterministic: 'requires deterministic routing', decisionsExplainable: 'requires explainable decisions', assignmentsIdempotent: 'requires idempotent assignments', escalationsAuthorized: 'requires authorized escalation' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', stalePresenceTreatedAsCurrent: 'prohibits stale presence as current', capacityExceeded: 'prohibits routing beyond capacity', unexplainedRouting: 'prohibits unexplained routing', duplicateAssignmentAccepted: 'prohibits duplicate assignment acceptance', unauthorizedEscalation: 'prohibits unauthorized escalation', routingOwnsCustomerOrTransactionState: 'prohibits routing ownership of customer or transaction state' });
export class ContactCenterQueuesRoutingPresenceDescriptor {
  responsibilities() { return values(metadata.responsibilities); } queueFields() { return values(metadata.queueFields); } skillFields() { return values(metadata.skillFields); } priorityFields() { return values(metadata.priorityFields); } assignmentFields() { return values(metadata.assignmentFields); } presenceFields() { return values(metadata.presenceFields); } capacityFields() { return values(metadata.capacityFields); } routingDecisionFields() { return values(metadata.routingDecisionFields); } escalationFields() { return values(metadata.escalationFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Contact Center Queues Routing Presence profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-04 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-04 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.CONTACT_CENTER_QUEUES_ROUTING_PRESENCE_ERROR_CODE, 'Contact Center Queues Routing Presence violates ARCH-027-04.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
