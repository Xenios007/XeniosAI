import { PlatformError } from '../../foundation/errors/platform-error.js'; import { ChannelConversationContextContinuityProfile as Profile } from '../contracts/channel-conversation-context-continuity-profile.js'; import * as constants from '../channel-conversation-context-continuity-constants.js';
const metadata = Object.freeze({ responsibilities: constants.CONTINUITY_RESPONSIBILITIES, channelIdentityFields: constants.CHANNEL_IDENTITY_FIELDS, conversationThreadFields: constants.CONVERSATION_THREAD_FIELDS, contextContinuityFields: constants.CONTEXT_CONTINUITY_FIELDS, interactionCorrelationFields: constants.INTERACTION_CORRELATION_FIELDS, conversationTransferFields: constants.CONVERSATION_TRANSFER_FIELDS, interactionHistoryFields: constants.INTERACTION_HISTORY_FIELDS, controls: constants.CONTINUITY_CONTROLS, failureRecovery: constants.CONTINUITY_FAILURE_RECOVERY, observabilityFields: constants.CONTINUITY_OBSERVABILITY_FIELDS, assuranceEvidence: constants.CONTINUITY_ASSURANCE_EVIDENCE, invariants: constants.CONTINUITY_INVARIANTS });
const required = Object.freeze({ identitiesNormalized: 'requires normalized channel identities', continuityPurposeScoped: 'requires purpose-scoped continuity', correlationStable: 'requires stable correlation', transfersAuthorized: 'requires authorized transfers', historyProvenanceRetained: 'requires retained history provenance', consentEnforced: 'requires consent and preference enforcement', tenantIsolation: 'requires tenant isolation', providerOwnsValidation: 'requires provider-owned validation', aiChangesOnlyThroughArch029: 'requires AI promotion through ARCH-029' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', contextCopiedWithoutPurpose: 'prohibits context transfer without purpose', transferWithoutAuthorization: 'prohibits unauthorized transfer', historyBecomesCustomerTruth: 'prohibits interaction history becoming customer truth', engagementOwnsChannelTransport: 'prohibits engagement ownership of channel transport', inferredIdentityAccepted: 'prohibits accepting inferred identity as verified identity' });
export class ChannelConversationContextContinuityDescriptor {
  responsibilities() { return values(metadata.responsibilities); } channelIdentityFields() { return values(metadata.channelIdentityFields); } conversationThreadFields() { return values(metadata.conversationThreadFields); } contextContinuityFields() { return values(metadata.contextContinuityFields); } interactionCorrelationFields() { return values(metadata.interactionCorrelationFields); } conversationTransferFields() { return values(metadata.conversationTransferFields); } interactionHistoryFields() { return values(metadata.interactionHistoryFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Channel Conversation Context Continuity profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-02 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-02 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.CHANNEL_CONVERSATION_CONTEXT_CONTINUITY_ERROR_CODE, 'Channel Conversation Context Continuity violates ARCH-027-02.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
