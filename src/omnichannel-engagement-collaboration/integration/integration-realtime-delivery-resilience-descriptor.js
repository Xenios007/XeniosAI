import { PlatformError } from '../../foundation/errors/platform-error.js'; import { IntegrationRealtimeDeliveryResilienceProfile as Profile } from '../contracts/integration-realtime-delivery-resilience-profile.js'; import * as constants from '../integration-realtime-delivery-resilience-constants.js';
const metadata = Object.freeze({ responsibilities: constants.REALTIME_INTEGRATION_RESPONSIBILITIES, adapterFields: constants.CHANNEL_ADAPTER_FIELDS, eventFields: constants.ENGAGEMENT_EVENT_FIELDS, presenceFields: constants.REALTIME_PRESENCE_FIELDS, streamFields: constants.STREAM_FIELDS, webhookFields: constants.WEBHOOK_FIELDS, outageBehaviorFields: constants.OUTAGE_BEHAVIOR_FIELDS, reconciliationFields: constants.RECONCILIATION_FIELDS, controls: constants.INTEGRATION_CONTROLS, failureRecovery: constants.INTEGRATION_FAILURE_RECOVERY, observabilityFields: constants.INTEGRATION_OBSERVABILITY, assuranceEvidence: constants.INTEGRATION_ASSURANCE, invariants: constants.INTEGRATION_INVARIANTS });
const required = Object.freeze({ adaptersVersioned: 'requires versioned adapters', credentialsIsolated: 'requires credential isolation', signaturesVerified: 'requires signature verification', deliveriesIdempotent: 'requires idempotent delivery', orderingExplicit: 'requires explicit ordering', backpressureBounded: 'requires bounded backpressure', freshnessExplicit: 'requires explicit freshness', outagesExplicit: 'requires explicit outages', reconciliationRequired: 'requires reconciliation', transportAckNotCompletion: 'requires transport acknowledgement distinct from completion' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', unsignedWebhookAccepted: 'prohibits unsigned webhooks', unboundedRetry: 'prohibits unbounded retry', stalePresenceAuthoritative: 'prohibits stale presence authority', outageHidden: 'prohibits hidden outage state', replayWithoutCheckpoint: 'prohibits replay without checkpoints', transportAckMeansCompletion: 'prohibits transport acknowledgement as business completion', adapterOwnsBusinessState: 'prohibits adapters owning business state' });
export class IntegrationRealtimeDeliveryResilienceDescriptor {
  responsibilities() { return values(metadata.responsibilities); } adapterFields() { return values(metadata.adapterFields); } eventFields() { return values(metadata.eventFields); } presenceFields() { return values(metadata.presenceFields); } streamFields() { return values(metadata.streamFields); } webhookFields() { return values(metadata.webhookFields); } outageBehaviorFields() { return values(metadata.outageBehaviorFields); } reconciliationFields() { return values(metadata.reconciliationFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Integration Realtime Delivery Resilience profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-08 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-08 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.INTEGRATION_REALTIME_DELIVERY_RESILIENCE_ERROR_CODE, 'Integration Realtime Delivery Resilience violates ARCH-027-08.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
