import { PlatformError } from '../../foundation/errors/platform-error.js'; import { CollaborationApprovalsHumanAgentHandoffProfile as Profile } from '../contracts/collaboration-approvals-human-agent-handoff-profile.js'; import * as constants from '../collaboration-approvals-human-agent-handoff-constants.js';
const metadata = Object.freeze({ responsibilities: constants.COLLABORATION_RESPONSIBILITIES, sharedWorkFields: constants.SHARED_WORK_FIELDS, commentFields: constants.COMMENT_FIELDS, approvalFields: constants.APPROVAL_FIELDS, transferFields: constants.TRANSFER_FIELDS, contextPackageFields: constants.CONTEXT_PACKAGE_FIELDS, handoffFields: constants.HUMAN_AGENT_HANDOFF_FIELDS, accountabilityFields: constants.ACCOUNTABILITY_FIELDS, controls: constants.COLLABORATION_CONTROLS, failureRecovery: constants.COLLABORATION_FAILURE_RECOVERY, observabilityFields: constants.COLLABORATION_OBSERVABILITY, assuranceEvidence: constants.COLLABORATION_ASSURANCE, invariants: constants.COLLABORATION_INVARIANTS });
const required = Object.freeze({ participantAccessEnforced: 'requires participant access enforcement', commentVisibilityEnforced: 'requires comment visibility enforcement', approvalAuthorityVerified: 'requires verified approval authority', separationOfDutiesEnforced: 'requires separation of duties', contextMinimizedAndGrounded: 'requires minimized grounded context', handoffAcceptanceExplicit: 'requires explicit handoff acceptance', humanAuthorityPreserved: 'requires preserved human authority', accountabilityRecorded: 'requires accountability records' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', approvalWithoutAuthority: 'prohibits approval without authority', expiredApprovalAccepted: 'prohibits accepting expired approval', contextCopiedWithoutPurpose: 'prohibits context transfer without purpose', handoffAssumedComplete: 'prohibits assumed handoff completion', agentOverridesHumanAuthority: 'prohibits agents overriding human authority', engagementOwnsAgentExecution: 'prohibits engagement ownership of agent execution', engagementOwnsWorkflowExecution: 'prohibits engagement ownership of workflow execution' });
export class CollaborationApprovalsHumanAgentHandoffDescriptor {
  responsibilities() { return values(metadata.responsibilities); } sharedWorkFields() { return values(metadata.sharedWorkFields); } commentFields() { return values(metadata.commentFields); } approvalFields() { return values(metadata.approvalFields); } transferFields() { return values(metadata.transferFields); } contextPackageFields() { return values(metadata.contextPackageFields); } handoffFields() { return values(metadata.handoffFields); } accountabilityFields() { return values(metadata.accountabilityFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Collaboration Approvals Human Agent Handoff profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-06 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-06 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.COLLABORATION_APPROVALS_HUMAN_AGENT_HANDOFF_ERROR_CODE, 'Collaboration Approvals Human Agent Handoff violates ARCH-027-06.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
