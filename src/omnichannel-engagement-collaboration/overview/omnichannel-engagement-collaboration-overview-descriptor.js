import { PlatformError } from '../../foundation/errors/platform-error.js';
import { OmnichannelEngagementCollaborationOverviewProfile as Profile } from '../contracts/omnichannel-engagement-collaboration-overview-profile.js';
import * as constants from '../omnichannel-engagement-collaboration-overview-constants.js';
const metadata = Object.freeze({ responsibilities: constants.ENGAGEMENT_RESPONSIBILITIES, authorities: constants.ENGAGEMENT_AUTHORITIES, capabilities: constants.ENGAGEMENT_CAPABILITIES, contractFields: constants.ENGAGEMENT_CONTRACT_FIELDS, aggregateFields: constants.ENGAGEMENT_AGGREGATE_FIELDS, lifecycleStates: constants.ENGAGEMENT_LIFECYCLE_STATES, governanceControls: constants.ENGAGEMENT_GOVERNANCE_CONTROLS, failureRecovery: constants.ENGAGEMENT_FAILURE_RECOVERY, observabilityFields: constants.ENGAGEMENT_OBSERVABILITY_FIELDS, assuranceEvidence: constants.ENGAGEMENT_ASSURANCE_EVIDENCE, invariants: constants.ENGAGEMENT_INVARIANTS });
const required = Object.freeze({ engagementOwnsCoordinatedCommunication: 'requires authoritative coordinated communication', engagementOwnsServiceWork: 'requires authoritative coordinated service work', providerOwnsValidation: 'requires provider-owned validation', purposeScopedContracts: 'requires purpose-scoped contracts', independentScaling: 'requires independent command and query scaling', extensionsGoverned: 'requires governed extensions', correctionAndRecoveryPreserved: 'requires correction and recovery', commandsAuthorized: 'requires authorized commands', queriesMinimumNecessary: 'requires minimum-necessary queries', eventsMinimumFacts: 'requires minimum-fact events', crossDomainWorkflows: 'requires workflow-based cross-domain work', consumersCannotInferCompletion: 'prohibits inferred completion', defaultDeny: 'requires default deny', tenantIsolation: 'requires tenant isolation', dataGoverned: 'requires governed data', aiChangesOnlyThroughArch029: 'requires AI promotion through ARCH-029' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', engagementOwnsChannelTransport: 'prohibits ownership of channel transport', engagementOwnsWorkflowExecution: 'prohibits ownership of workflow execution', engagementOwnsAgentRuntime: 'prohibits ownership of agent runtime', engagementOwnsCustomerTruth: 'prohibits ownership of customer truth', engagementOwnsTransactionState: 'prohibits ownership of transaction state', generatedCompletionAccepted: 'prohibits generated or optimistic completion' });
export class OmnichannelEngagementCollaborationOverviewDescriptor {
  responsibilities() { return values(metadata.responsibilities); } authorities() { return values(metadata.authorities); } capabilities() { return values(metadata.capabilities); } contractFields() { return values(metadata.contractFields); } aggregateFields() { return values(metadata.aggregateFields); } lifecycleStates() { return values(metadata.lifecycleStates); } governanceControls() { return values(metadata.governanceControls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Omnichannel Engagement Collaboration Overview profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-01 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-01 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.OMNICHANNEL_ENGAGEMENT_COLLABORATION_OVERVIEW_ERROR_CODE, 'Omnichannel Engagement Collaboration Overview violates ARCH-027-01.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
