import { PlatformError } from '../../foundation/errors/platform-error.js'; import { CampaignsEngagementJourneysProfile as Profile } from '../contracts/campaigns-engagement-journeys-profile.js'; import * as constants from '../campaigns-engagement-journeys-constants.js';
const metadata = Object.freeze({ responsibilities: constants.CAMPAIGN_RESPONSIBILITIES, campaignFields: constants.CAMPAIGN_FIELDS, audienceActivationFields: constants.AUDIENCE_ACTIVATION_FIELDS, frequencyPolicyFields: constants.FREQUENCY_POLICY_FIELDS, journeyFields: constants.JOURNEY_FIELDS, journeyStepFields: constants.JOURNEY_STEP_FIELDS, measurementFields: constants.ENGAGEMENT_MEASUREMENT_FIELDS, stopConditionFields: constants.STOP_CONDITION_FIELDS, controls: constants.CAMPAIGN_CONTROLS, failureRecovery: constants.CAMPAIGN_FAILURE_RECOVERY, observabilityFields: constants.CAMPAIGN_OBSERVABILITY, assuranceEvidence: constants.CAMPAIGN_ASSURANCE, invariants: constants.CAMPAIGN_INVARIANTS });
const required = Object.freeze({ campaignsApproved: 'requires campaign approval', audiencesVersioned: 'requires versioned audiences', consentAndSuppressionEnforced: 'requires consent and suppression enforcement', frequencyEnforced: 'requires frequency enforcement', workflowAuthorityExternal: 'requires external workflow authority', measurementsProvenanced: 'requires provenanced measurements', stopConditionsDeterministic: 'requires deterministic stop conditions', activationsIdempotent: 'requires idempotent activation' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', activationWithoutConsent: 'prohibits activation without consent', frequencyLimitBypassed: 'prohibits bypassing frequency limits', inferredAudienceAccepted: 'prohibits inferred audiences as authoritative', workflowExecutionOwnedHere: 'prohibits owning workflow execution', measurementBecomesSourceTruth: 'prohibits measurements becoming source truth', stopConditionIgnored: 'prohibits ignored stop conditions', aiPublishesCampaign: 'prohibits AI campaign publication' });
export class CampaignsEngagementJourneysDescriptor {
  responsibilities() { return values(metadata.responsibilities); } campaignFields() { return values(metadata.campaignFields); } audienceActivationFields() { return values(metadata.audienceActivationFields); } frequencyPolicyFields() { return values(metadata.frequencyPolicyFields); } journeyFields() { return values(metadata.journeyFields); } journeyStepFields() { return values(metadata.journeyStepFields); } measurementFields() { return values(metadata.measurementFields); } stopConditionFields() { return values(metadata.stopConditionFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Campaigns Engagement Journeys profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-07 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-07 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.CAMPAIGNS_ENGAGEMENT_JOURNEYS_ERROR_CODE, 'Campaigns Engagement Journeys violates ARCH-027-07.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
