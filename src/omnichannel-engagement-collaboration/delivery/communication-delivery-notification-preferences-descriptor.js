import { PlatformError } from '../../foundation/errors/platform-error.js'; import { CommunicationDeliveryNotificationPreferencesProfile as Profile } from '../contracts/communication-delivery-notification-preferences-profile.js'; import * as constants from '../communication-delivery-notification-preferences-constants.js';
const metadata = Object.freeze({ responsibilities: constants.DELIVERY_RESPONSIBILITIES, messageIntentFields: constants.MESSAGE_INTENT_FIELDS, templateFields: constants.COMMUNICATION_TEMPLATE_FIELDS, deliveryAttemptFields: constants.DELIVERY_ATTEMPT_FIELDS, receiptFields: constants.DELIVERY_RECEIPT_FIELDS, retryFields: constants.DELIVERY_RETRY_FIELDS, preferenceFields: constants.COMMUNICATION_PREFERENCE_FIELDS, consentSuppressionFields: constants.CONSENT_SUPPRESSION_FIELDS, controls: constants.DELIVERY_CONTROLS, failureRecovery: constants.DELIVERY_FAILURE_RECOVERY, observabilityFields: constants.DELIVERY_OBSERVABILITY_FIELDS, assuranceEvidence: constants.DELIVERY_ASSURANCE_EVIDENCE, invariants: constants.DELIVERY_INVARIANTS });
const required = Object.freeze({ purposeValidated: 'requires validated purpose', templatesVersionedAndApproved: 'requires versioned approved templates', preferencesAuthoritative: 'requires authoritative preference references', consentAuthoritative: 'requires authoritative consent references', suppressionChecked: 'requires suppression checks', deliveryIdempotent: 'requires idempotent delivery', receiptsVerified: 'requires verified receipts', retriesBounded: 'requires bounded retries', deliveryStatusExplicit: 'requires explicit delivery status', tenantIsolation: 'requires tenant isolation' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', deliveryWithoutConsentCheck: 'prohibits delivery without consent checks', deliveryDespiteSuppression: 'prohibits delivery despite suppression', unboundedRetry: 'prohibits unbounded retry', optimisticDeliveryAssumed: 'prohibits optimistic delivery assumptions', engagementOwnsChannelTransport: 'prohibits engagement ownership of channel transport', engagementOwnsPreferenceTruth: 'prohibits engagement ownership of preference truth' });
export class CommunicationDeliveryNotificationPreferencesDescriptor {
  responsibilities() { return values(metadata.responsibilities); } messageIntentFields() { return values(metadata.messageIntentFields); } templateFields() { return values(metadata.templateFields); } deliveryAttemptFields() { return values(metadata.deliveryAttemptFields); } receiptFields() { return values(metadata.receiptFields); } retryFields() { return values(metadata.retryFields); } preferenceFields() { return values(metadata.preferenceFields); } consentSuppressionFields() { return values(metadata.consentSuppressionFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Communication Delivery Notification Preferences profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-03 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-03 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.COMMUNICATION_DELIVERY_NOTIFICATION_PREFERENCES_ERROR_CODE, 'Communication Delivery Notification Preferences violates ARCH-027-03.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
