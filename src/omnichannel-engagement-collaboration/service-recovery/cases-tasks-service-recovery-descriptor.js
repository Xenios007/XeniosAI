import { PlatformError } from '../../foundation/errors/platform-error.js'; import { CasesTasksServiceRecoveryProfile as Profile } from '../contracts/cases-tasks-service-recovery-profile.js'; import * as constants from '../cases-tasks-service-recovery-constants.js';
const metadata = Object.freeze({ responsibilities: constants.SERVICE_RECOVERY_RESPONSIBILITIES, caseFields: constants.CASE_FIELDS, lifecycleStates: constants.CASE_LIFECYCLE_STATES, taskFields: constants.SERVICE_TASK_FIELDS, serviceLevelFields: constants.SERVICE_LEVEL_FIELDS, ownershipFields: constants.OWNERSHIP_FIELDS, complaintFields: constants.COMPLAINT_FIELDS, disputeReferenceFields: constants.DISPUTE_REFERENCE_FIELDS, remediationFields: constants.REMEDIATION_FIELDS, resolutionFields: constants.RESOLUTION_FIELDS, controls: constants.SERVICE_RECOVERY_CONTROLS, failureRecovery: constants.SERVICE_RECOVERY_FAILURES, observabilityFields: constants.SERVICE_RECOVERY_OBSERVABILITY, assuranceEvidence: constants.SERVICE_RECOVERY_ASSURANCE, invariants: constants.SERVICE_RECOVERY_INVARIANTS });
const required = Object.freeze({ caseOwnershipAuthoritative: 'requires authoritative case ownership', taskOwnershipAuthoritative: 'requires authoritative task ownership', slaDeterministic: 'requires deterministic SLA calculation', ownershipTransitionsAuthorized: 'requires authorized ownership transitions', disputeAuthorityExternal: 'requires external dispute authority', remediationApprovedAndBounded: 'requires approved bounded remediation', resolutionEvidenceRequired: 'requires resolution evidence', humanReviewAvailable: 'requires human review and appeal' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', workflowStateOwnedHere: 'prohibits ownership of workflow execution state', transactionDisputeOwnedHere: 'prohibits ownership of transaction or financial disputes', remediationWithoutApproval: 'prohibits remediation without approval', resolutionWithoutEvidence: 'prohibits resolution without evidence', silentSlaBreach: 'prohibits silent SLA breaches', inferredCompletionAccepted: 'prohibits inferred completion' });
export class CasesTasksServiceRecoveryDescriptor {
  responsibilities() { return values(metadata.responsibilities); } caseFields() { return values(metadata.caseFields); } lifecycleStates() { return values(metadata.lifecycleStates); } taskFields() { return values(metadata.taskFields); } serviceLevelFields() { return values(metadata.serviceLevelFields); } ownershipFields() { return values(metadata.ownershipFields); } complaintFields() { return values(metadata.complaintFields); } disputeReferenceFields() { return values(metadata.disputeReferenceFields); } remediationFields() { return values(metadata.remediationFields); } resolutionFields() { return values(metadata.resolutionFields); } controls() { return values(metadata.controls); } failureRecovery() { return values(metadata.failureRecovery); } observabilityFields() { return values(metadata.observabilityFields); } assuranceEvidence() { return values(metadata.assuranceEvidence); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Cases Tasks Service Recovery profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-05 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-05 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.CASES_TASKS_SERVICE_RECOVERY_ERROR_CODE, 'Cases Tasks Service Recovery violates ARCH-027-05.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
