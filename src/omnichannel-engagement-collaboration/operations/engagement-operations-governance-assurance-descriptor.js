import { PlatformError } from '../../foundation/errors/platform-error.js'; import { EngagementOperationsGovernanceAssuranceProfile as Profile } from '../contracts/engagement-operations-governance-assurance-profile.js'; import * as constants from '../engagement-operations-governance-assurance-constants.js';
const metadata = Object.freeze({ operatingRoles: constants.ENGAGEMENT_OPERATING_ROLES, qualityObjectives: constants.ENGAGEMENT_QUALITY_OBJECTIVES, telemetryFields: constants.ENGAGEMENT_OPERATIONS_TELEMETRY, abuseControls: constants.ENGAGEMENT_ABUSE_CONTROLS, incidentClasses: constants.ENGAGEMENT_INCIDENT_CLASSES, incidentEvidence: constants.ENGAGEMENT_INCIDENT_EVIDENCE, governanceArtifacts: constants.ENGAGEMENT_GOVERNANCE_ARTIFACTS, assuranceActivities: constants.ENGAGEMENT_ASSURANCE_ACTIVITIES, invariants: constants.ENGAGEMENT_OPERATIONS_INVARIANTS });
const required = Object.freeze({ accountableOwnership: 'requires accountable ownership', qualityObjectivesMeasured: 'requires measured quality objectives', protectedPayloadsExcludedFromTelemetry: 'requires protected payload exclusion from telemetry', abuseControlsBounded: 'requires bounded abuse controls', humanReviewAvailable: 'requires human review and appeal', incidentsReconciled: 'requires incident reconciliation', evidenceRetained: 'requires retained evidence', assuranceContinuous: 'requires continuous assurance' });
const prohibited = Object.freeze({ sharedDatabases: 'prohibits shared databases', protectedPayloadsCopiedToTelemetry: 'prohibits protected payloads in telemetry', abuseSignalChangesAuthoritativeState: 'prohibits abuse signals silently changing authoritative state', consentBypassedUnderPressure: 'prohibits consent bypass under pressure', tenantControlBypassedUnderPressure: 'prohibits tenant-control bypass under pressure', incidentEvidenceDiscarded: 'prohibits discarding incident evidence', externalAuthorityAbsorbed: 'prohibits absorbing external authority' });
export class EngagementOperationsGovernanceAssuranceDescriptor {
  operatingRoles() { return values(metadata.operatingRoles); } qualityObjectives() { return values(metadata.qualityObjectives); } telemetryFields() { return values(metadata.telemetryFields); } abuseControls() { return values(metadata.abuseControls); } incidentClasses() { return values(metadata.incidentClasses); } incidentEvidence() { return values(metadata.incidentEvidence); } governanceArtifacts() { return values(metadata.governanceArtifacts); } assuranceActivities() { return values(metadata.assuranceActivities); } invariants() { return values(metadata.invariants); }
  validateProfile(input) { const profile = input instanceof Profile ? input : new Profile(input); const errors = []; if (!profile.profileName) errors.push('Engagement Operations Governance Assurance profile must have a name.'); for (const [key, source] of Object.entries(metadata)) for (const item of values(source)) if (!profile[key].includes(item)) errors.push(`${key} must include ${item}.`); for (const [key, message] of Object.entries(required)) if (!profile[key]) errors.push(`ARCH-027-09 ${message}.`); for (const [key, message] of Object.entries(prohibited)) if (profile[key]) errors.push(`ARCH-027-09 ${message}.`); return Object.freeze({ isValid: errors.length === 0, errors: Object.freeze(errors) }); }
  assertArchitecture() { const errors = []; for (const [key, source] of Object.entries(metadata)) if (this[key]().length !== Object.keys(source).length) errors.push(`${key} is incomplete.`); if (errors.length) throw new PlatformError(constants.ENGAGEMENT_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE, 'Engagement Operations Governance Assurance violates ARCH-027-09.', { errors }); return Object.freeze({ isValid: true, errors: Object.freeze([]) }); }
}
function values(source) { return Object.freeze(Object.values(source)); }
