# ARCH-019-03 Package, Manifest, Registry, and Provenance

`PackageManifestRegistryProvenanceDescriptor` exposes immutable metadata for supply-chain principles, package contents, manifest fields, capability declaration types, data-handling declaration fields, configuration schema attributes, entry-point fields, package dependency types, dependency inventory fields, extension dependency requirements, publisher identity elements, publishing authority controls, build provenance links, supply-chain checks, signature verification checks, key lifecycle requirements, extension registry responsibilities, registry boundaries, registry namespace fields, package publication states, distribution verification checks, package advisory fields, emergency revocation actions, registry recovery coverage, supply-chain evidence types, and architectural rules.

Profile validation requires immutable digest-bound package versions, authority-free manifests, separated publisher and runtime identity, traceable provenance, explicit inspectable dependencies, integrity-only signatures, authoritative registries, exact-digest distribution verification, rapidly actionable vulnerable packages, secret-free packages and provenance, mandatory-field manifest validation, unloaded undeclared entry points, non-inherited extension capabilities, equally controlled private packages, and fail-closed unavailable-registry behavior. It rejects permitted undeclared data collection, floating production dependencies, ownership implied by similar names, reduced controls for private locations, executable revoked package bytes, signatures replacing capability enforcement, and unaudited local publication for moderate/high/restricted-risk packages.

The module establishes trustworthy extension identity and integrity — immutable packages, declarative manifests, explicit dependencies, verified publishers, attestable builds, signatures, authoritative registries, advisories, and revocation — without confusing provenance with runtime authority.
