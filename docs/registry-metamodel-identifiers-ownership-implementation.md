# ARCH-034-02 Registry Metamodel, Identifiers & Ownership

Executable metadata and validation for artifact identity, kind, accountable ownership, namespace allocation, immutable versions, policy-sourced classification, provenance, and governed lifecycle.

Canonical identifiers are never reused, published versions are never mutated, ownership transfer is explicit and evidenced, namespaces remain tenant bounded, and none of this metadata grants provider runtime authority.
