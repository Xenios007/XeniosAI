# ARCH-018-07 Tenant-Aware Runtime and Integration

`TenantAwareRuntimeIntegrationDescriptor` exposes immutable metadata for runtime principles, runtime context fields, runtime trust boundaries, context establishment steps, context representations, experience layer requirements, gateway responsibilities, API scope classifications, SDK behaviors, provider responsibility steps, service-to-service propagated fields, data access patterns, cache access rules, event envelope fields, event consumption steps, batch job fields, workflow binding continuity, orchestrator binding targets, agent grant elements, memory scope levels, integration connection fields, outbound validation factors, extension runtime controls, operator tool capabilities, context expiry triggers, runtime observability signals, runtime failure behaviors, conformance testing areas, and architectural rules.

Profile validation requires explicit trusted context on every execution, provider revalidation, boundary-surviving context, narrowing-only delegation, authoritative-placement routing, minimized and protected context, revocation-invalidated context, validation that caches and retries cannot bypass, tenant-safe telemetry, provider-owned resource validation beneath gateway-established context, revalidating async retries, fail-closed knowledge filtering, explicit extension capability grants, checkpointed long-running revocation checks, and cross-checked payload tenant identifiers. It rejects unsafe default tenants, treating client input as authority, unvalidated forwarding of raw headers as trusted context, SDKs inferring tenant from local state, callers minting broader downstream authority, cache fallback to another tenant's value, model output altering tenant context, failure fallback to a default tenant, and hidden impersonation in operator tools.

The module makes tenant isolation an execution invariant across experiences, APIs, SDKs, gateways, services, data access, caches, events, queues, jobs, workflows, AI orchestrators, agents, knowledge, memory, integrations, extensions, and operational tooling.
