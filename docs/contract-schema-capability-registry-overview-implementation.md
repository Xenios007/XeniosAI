# ARCH-034-01 Contract, Schema & Capability Registry Overview

Executable registry artifact, record, consumer, boundary, quality, context, discovery, validation, lineage, lifecycle, federation, recovery, observability, and assurance metadata.

The registry owns discovery and assurance metadata only. Contract and platform owners retain semantics and runtime behavior; visibility is never permission, partial failure remains explicit, and machine assistance cannot self-publish or expand authority.
