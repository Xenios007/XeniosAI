import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{AI_AGENT_KNOWLEDGE_UI_TESTING_ERROR_CODE,AiAgentKnowledgeUiTestingDescriptor as D,AiAgentKnowledgeUiTestingProfile as P}from'../../src/testing-simulation-conformance/index.js';const K=['deterministicAgentTests','retrievalGroundingTests','knowledgeVersionTests','uiAccessibilityTests','visualRegressionTests','interactionTests','boundaryFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI ai and ui testing',...x})};test('ARCH-037-07 exposes agent grounding knowledge accessibility visual interaction and boundary metadata',()=>{const d=new D();assert.ok(d.deterministicAgentTests().includes('recorded-and-replayed-model-responses-with-no-live-model-in-deterministic-tests'));assert.ok(d.retrievalGroundingTests().includes('unsupported-and-fabricated-claim-detection-tests'));assert.ok(d.knowledgeVersionTests().includes('knowledge-base-and-index-version-pinned-per-test-run'));assert.ok(d.uiAccessibilityTests().includes('screen-reader-label-role-and-announcement-tests'));assert.ok(d.visualRegressionTests().includes('human-review-required-to-accept-an-intended-visual-change'));assert.ok(d.interactionTests().includes('incremental-and-streaming-response-rendering-tests'));assert.ok(d.boundaryFields().includes('production-ai-evaluation-learning-and-promotion-remain-owned-by-arch-029'));assert.ok(d.invariants().includes('a-deterministic-agent-or-tool-test-never-calls-a-live-model-endpoint'))});test('ARCH-037-07 validates live-model grounding knowledge accessibility visual and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({liveModelInDeterministicTest:true,ungroundedResponseUnflagged:true,knowledgeVersionUnpinned:true,accessibilityRegressionMerged:true,visualDiffAutoAccepted:true,productionAiEvalRunHere:true,aiSilentlyLowersGroundingBar:true,flakyAgentTestIgnored:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a deterministic agent or tool test calling a live model endpoint/);assert.match(r.errors.join('\n'),/leaving an ungrounded or fabricated response claim unflagged/);assert.match(r.errors.join('\n'),/this capability performing production AI evaluation learning or promotion owned by ARCH-029/);assert.match(r.errors.join('\n'),/auto-accepting an intended visual change without human review/);const m=d.validateProfile({profileName:'x',agentTestsDeterministic:false,groundingVerified:false,visualRegressionGated:false});assert.match(m.errors.join('\n'),/requires agent and tool tests to use recorded model responses and be seeded and reproducible/);assert.match(m.errors.join('\n'),/requires visual regressions gated with human review for an intended change/)});test('ARCH-037-07 detects incomplete metadata and preserves immutability',()=>{class I extends D{knowledgeVersionTests(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===AI_AGENT_KNOWLEDGE_UI_TESTING_ERROR_CODE);assert.throws(()=>complete(new D()).retrievalGroundingTests.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
