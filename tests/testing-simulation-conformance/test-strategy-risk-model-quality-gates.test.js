import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{TEST_STRATEGY_RISK_MODEL_QUALITY_GATES_ERROR_CODE,TestStrategyRiskModelQualityGatesDescriptor as D,TestStrategyRiskModelQualityGatesProfile as P}from'../../src/testing-simulation-conformance/index.js';const K=['testPortfolioFields','riskTierFields','coverageTargetFields','qualityGateFields','gateOwnershipFields','waiverFields','releaseEvidenceFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI test strategy',...x})};test('ARCH-037-02 exposes portfolio risk-tier coverage gate ownership waiver and release-evidence metadata',()=>{const d=new D();assert.ok(d.testPortfolioFields().includes('test-maintenance-budget-and-deprecation-of-low-value-tests'));assert.ok(d.riskTierFields().includes('every-change-and-component-is-assigned-a-risk-tier'));assert.ok(d.coverageTargetFields().includes('non-negotiable-minimum-bars-for-critical-and-high-tiers'));assert.ok(d.qualityGateFields().includes('failing-blocking-criteria-stop-progression'));assert.ok(d.gateOwnershipFields().includes('defined-authority-to-approve-overrides-and-waivers'));assert.ok(d.waiverFields().includes('compensating-controls-monitoring-and-follow-up-commitments'));assert.ok(d.releaseEvidenceFields().includes('immutable-and-integrity-protected-evidence-bundle'));assert.ok(d.invariants().includes('a-release-never-proceeds-without-a-complete-traceable-evidence-bundle-and-accountable-sign-off'))});test('ARCH-037-02 validates gate bypass coverage waiver release evidence and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({gateBypassedSilently:true,coverageTargetUnenforced:true,permanentWaiver:true,waiverWithoutCompensation:true,releaseWithoutEvidence:true,aiSilentlyWaivesGate:true,riskTierUnassigned:true,evidenceFabricated:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/bypassing a quality gate without an authorized audited override or waiver/);assert.match(r.errors.join('\n'),/a permanent or non-expiring waiver/);assert.match(r.errors.join('\n'),/AI assistance silently waiving a gate lowering a coverage target or signing off a release/);assert.match(r.errors.join('\n'),/fabricated or backfilled release evidence/);const m=d.validateProfile({profileName:'x',riskTiered:false,gatesBlocking:false,releaseEvidenceComplete:false});assert.match(m.errors.join('\n'),/requires every change and component assigned a documented risk tier/);assert.match(m.errors.join('\n'),/requires a complete traceable evidence bundle and sign-off before release/)});test('ARCH-037-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{qualityGateFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===TEST_STRATEGY_RISK_MODEL_QUALITY_GATES_ERROR_CODE);assert.throws(()=>complete(new D()).riskTierFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
