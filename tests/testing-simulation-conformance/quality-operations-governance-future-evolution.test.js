import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{QUALITY_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE,QualityOperationsGovernanceFutureEvolutionDescriptor as D,QualityOperationsGovernanceFutureEvolutionProfile as P}from'../../src/testing-simulation-conformance/index.js';const K=['platformOperationsFields','flakyTestControlFields','qualityAnalyticsFields','evidenceRetentionFields','federatedQualityFields','futureEvolutionFields','ownershipGovernanceFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI quality operations',...x})};test('ARCH-037-10 exposes platform flake analytics retention federation future-evolution and governance metadata',()=>{const d=new D();assert.ok(d.platformOperationsFields().includes('result-and-artifact-caching-keyed-by-inputs-and-versions'));assert.ok(d.flakyTestControlFields().includes('quarantined-tests-still-run-and-report-without-blocking'));assert.ok(d.qualityAnalyticsFields().includes('coverage-pass-rate-flake-rate-lead-time-and-escaped-defect-metrics'));assert.ok(d.evidenceRetentionFields().includes('legal-hold-application-and-release-with-no-deletion-under-hold'));assert.ok(d.federatedQualityFields().includes('aggregated-cross-team-quality-posture-without-cross-tenant-leakage'));assert.ok(d.futureEvolutionFields().includes('no-generated-test-is-merged-without-human-review'));assert.ok(d.ownershipGovernanceFields().includes('escalation-path-for-blocked-gates-and-quality-disputes'));assert.ok(d.invariants().includes('an-ai-generated-test-is-never-merged-without-human-review-and-assistance-never-expands-authority'))});test('ARCH-037-10 validates flake retention analytics federation governance and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({flakyTestLeftUnowned:true,evidenceRetainedUnbounded:true,analyticsLeaksSecrets:true,federatedQualityCrossTenantLeak:true,aiSilentlyGeneratesAndMergesTests:true,qualityGovernanceBypassed:true,evidenceDeletedUnderHold:true,futureFeatureExpandsAuthority:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/leaving a flaky test unowned or allowing it to silently block or hide signal/);assert.match(r.errors.join('\n'),/deleting evidence under a legal hold/);assert.match(r.errors.join('\n'),/AI assistance silently generating and merging tests or bypassing a quality gate/);assert.match(r.errors.join('\n'),/a future feature expanding authority beyond declared capabilities/);const m=d.validateProfile({profileName:'x',flakesControlled:false,evidenceRetentionBounded:false,humanInTheLoop:false});assert.match(m.errors.join('\n'),/requires flaky tests detected quarantined with an owner and deadline/);assert.match(m.errors.join('\n'),/requires proposal-only explainable bounded and human-reviewed AI-assisted test generation/)});test('ARCH-037-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{evidenceRetentionFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===QUALITY_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).flakyTestControlFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
