import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{ENVIRONMENT_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE,EnvironmentOperationsGovernanceFutureEvolutionDescriptor as D,EnvironmentOperationsGovernanceFutureEvolutionProfile as P}from'../../src/developer-environments-sandboxes/index.js';const K=['ownershipFields','observabilityOpsFields','incidentFields','conformanceFields','sustainabilityFields','federationFields','futureEvolutionFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI environment operations',...x})};test('ARCH-036-10 exposes ownership observability incident conformance sustainability federation and future-evolution metadata',()=>{const d=new D();assert.ok(d.ownershipFields().includes('team-onboarding-guidelines-and-golden-paths'));assert.ok(d.observabilityOpsFields().includes('boundary-verification-and-violation-signals'));assert.ok(d.incidentFields().includes('rebuild-from-specification-and-verification-after-containment'));assert.ok(d.conformanceFields().includes('conformance-gate-before-a-new-environment-class-or-template-is-published'));assert.ok(d.sustainabilityFields().includes('sustainability-claims-are-evidence-backed-and-not-overstated'));assert.ok(d.federationFields().includes('policy-driven-placement-honoring-residency-and-latency'));assert.ok(d.futureEvolutionFields().includes('ai-assisted-environment-operations-that-humans-approve'));assert.ok(d.invariants().includes('a-new-environment-class-or-template-never-bypasses-the-conformance-gate'))});test('ARCH-036-10 validates incident conformance telemetry federation sustainability and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({opsGrantsProductionAuthority:true,incidentSilentlyClosed:true,conformanceGateBypassed:true,telemetryLeaksSecrets:true,federationCrossTenantLeak:true,aiSilentlyOperatesFleet:true,unsustainableDefaultRetained:true,sustainabilityClaimUnverified:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/closing a sandbox incident without recovery verification and a blameless postmortem/);assert.match(r.errors.join('\n'),/a new environment class or template bypassing the conformance gate/);assert.match(r.errors.join('\n'),/overstated or unverified sustainability and efficiency claims/);assert.match(r.errors.join('\n'),/AI-assisted operations silently acting on the fleet/);const m=d.validateProfile({profileName:'x',ownershipAccountable:false,conformanceGated:false,humanInTheLoop:false});assert.match(m.errors.join('\n'),/requires an accountable owner and governing body/);assert.match(m.errors.join('\n'),/requires proposal-only explainable bounded and opt-in AI-assisted operations/)});test('ARCH-036-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{conformanceFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===ENVIRONMENT_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).sustainabilityFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
