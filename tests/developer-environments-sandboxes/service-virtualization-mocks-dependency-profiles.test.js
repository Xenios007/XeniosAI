import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{SERVICE_VIRTUALIZATION_MOCKS_DEPENDENCY_PROFILES_ERROR_CODE,ServiceVirtualizationMocksDependencyProfilesDescriptor as D,ServiceVirtualizationMocksDependencyProfilesProfile as P}from'../../src/developer-environments-sandboxes/index.js';const K=['simulatedDependencyFields','contractBindingFields','latencyProfileFields','failureInjectionFields','fixtureManagementFields','fidelityFields','driftDetectionFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI service virtualization',...x})};test('ARCH-036-05 exposes dependency contract latency failure fixture fidelity and drift metadata',()=>{const d=new D();assert.ok(d.simulatedDependencyFields().includes('runtime-switch-between-virtual-and-approved-non-production-real-endpoints'));assert.ok(d.contractBindingFields().includes('mocks-never-add-operations-fields-or-authority-absent-from-the-contract'));assert.ok(d.latencyProfileFields().includes('seeded-latency-profiles-for-reproducible-runs'));assert.ok(d.failureInjectionFields().includes('cascading-downstream-failure-simulation'));assert.ok(d.fixtureManagementFields().includes('fixtures-captured-only-from-approved-non-production-sources'));assert.ok(d.fidelityFields().includes('explicit-warning-that-virtual-fidelity-is-not-a-production-guarantee'));assert.ok(d.driftDetectionFields().includes('quarantine-of-profiles-that-drift-beyond-a-threshold'));assert.ok(d.invariants().includes('a-virtual-dependency-never-makes-a-call-to-a-production-system'))});test('ARCH-036-05 validates production isolation fidelity contract drift and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({virtualDependencyReachesProduction:true,fidelityAssumedComplete:true,staleFixtureUnlabeled:true,mockInventsContract:true,realCredentialsInMock:true,aiSilentlyAltersMock:true,driftIgnored:true,secretsInFixtures:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a virtual dependency making a call to a production system/);assert.match(r.errors.join('\n'),/a mock adding operations fields or authority absent from its bound contract/);assert.match(r.errors.join('\n'),/AI assistance silently altering a mock fixture latency profile or failure scenario/);assert.match(r.errors.join('\n'),/leaving a profile that drifts beyond threshold from its contract serving/);const m=d.validateProfile({profileName:'x',contractBound:false,driftDetected:false,labelledSimulated:false});assert.match(m.errors.join('\n'),/requires every mock bound to a registered contract and schema version/);assert.match(m.errors.join('\n'),/requires detection and alerting of virtual-to-contract drift/)});test('ARCH-036-05 detects incomplete metadata and preserves immutability',()=>{class I extends D{fidelityFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===SERVICE_VIRTUALIZATION_MOCKS_DEPENDENCY_PROFILES_ERROR_CODE);assert.throws(()=>complete(new D()).contractBindingFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
