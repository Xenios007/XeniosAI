import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PROJECT_SCAFFOLDING_TEMPLATES_CODE_GENERATION_ERROR_CODE,ProjectScaffoldingTemplatesCodeGenerationDescriptor as D,ProjectScaffoldingTemplatesCodeGenerationProfile as P}from'../../src/cli-ide-local-tooling/index.js';const K=['templateCatalogFields','templateInputFields','provenanceFields','deterministicGenerationFields','ownershipFields','regenerationFields','safeCustomizationFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI scaffolding',...x})};test('ARCH-035-04 exposes template catalog input provenance determinism ownership regeneration and customization metadata',()=>{const d=new D();assert.ok(d.templateCatalogFields().includes('authoritative-template-repository-digest-and-signature'));assert.ok(d.templateInputFields().includes('no-secret-parameters-accepted-as-plain-command-arguments'));assert.ok(d.provenanceFields().includes('template-identifier-and-version-stamped-in-generated-output'));assert.ok(d.deterministicGenerationFields().includes('identical-inputs-template-and-versions-produce-byte-identical-output'));assert.ok(d.ownershipFields().includes('clearly-marked-generated-and-editable-regions'));assert.ok(d.regenerationFields().includes('three-way-merge-of-template-changes-and-local-edits'));assert.ok(d.safeCustomizationFields().includes('protected-regions-that-regeneration-never-overwrites'));assert.ok(d.invariants().includes('regeneration-never-silently-overwrites-protected-regions-or-local-edits'))});test('ARCH-035-04 validates authority distinguishability determinism customization and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({generationGrantsAuthority:true,generatedIndistinguishable:true,secretsInGeneratedFiles:true,nondeterministicOutput:true,silentCustomizationOverwrite:true,unversionedTemplate:true,aiSilentlyGenerates:true,productionCredentialInScaffold:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/indistinguishable from hand-written or production truth/);assert.match(r.errors.join('\n'),/non-deterministic output with timestamps randomness or hostnames/);assert.match(r.errors.join('\n'),/regeneration silently overwriting protected regions or local edits/);assert.match(r.errors.join('\n'),/AI assistance silently generating or overwriting files/);const m=d.validateProfile({profileName:'x',templatesApproved:false,provenanceStamped:false,deterministicOutput:false});assert.match(m.errors.join('\n'),/requires approved signed and version-pinned templates/);assert.match(m.errors.join('\n'),/requires reproducible entropy-free generation/)});test('ARCH-035-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{provenanceFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PROJECT_SCAFFOLDING_TEMPLATES_CODE_GENERATION_ERROR_CODE);assert.throws(()=>complete(new D()).ownershipFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
