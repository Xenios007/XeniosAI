import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CONTRACT_SCHEMA_CLIENT_SERVER_GENERATION_ERROR_CODE,ContractSchemaClientServerGenerationDescriptor as D,ContractSchemaClientServerGenerationProfile as P}from'../../src/cli-ide-local-tooling/index.js';const K=['registryInputFields','languageTargetFields','compatibilityFields','generatedClientFields','stubValidatorFields','traceabilityFields','regenerationCompatFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI contract generation',...x})};test('ARCH-035-05 exposes registry input language target compatibility client stub traceability and regeneration metadata',()=>{const d=new D();assert.ok(d.registryInputFields().includes('explicitly-pinned-immutable-contract-and-schema-versions'));assert.ok(d.languageTargetFields().includes('idiomatic-naming-typing-and-error-mapping-per-language'));assert.ok(d.compatibilityFields().includes('detected-breaking-added-removed-and-narrowed-elements'));assert.ok(d.generatedClientFields().includes('authentication-injection-points-without-embedded-credentials'));assert.ok(d.stubValidatorFields().includes('authorization-hook-points-that-delegate-to-provider-services'));assert.ok(d.traceabilityFields().includes('element-level-map-from-contract-operation-to-generated-symbol'));assert.ok(d.regenerationCompatFields().includes('human-review-required-when-regeneration-is-breaking'));assert.ok(d.invariants().includes('generated-server-stubs-always-delegate-authorization-and-business-rules-to-provider-services'))});test('ARCH-035-05 validates authority delegation determinism credentials breaking-change and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({generatedGrantsAuthority:true,generatedIndistinguishable:true,credentialsInGeneratedClient:true,generatorInventsFields:true,breakingChangeUnflagged:true,aiSilentlyRegenerates:true,unpinnedRegistryInput:true,secretsInGeneratedArtifacts:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/generator inventing fields operations or authority not in the source contract/);assert.match(r.errors.join('\n'),/publishing a breaking generated-surface change without classification and review/);assert.match(r.errors.join('\n'),/AI assistance silently regenerating or publishing generated code/);assert.match(r.errors.join('\n'),/unpinned or unverified registry contract inputs/);const m=d.validateProfile({profileName:'x',registryPinned:false,compatibilityChecked:false,providerAuthorityPreserved:false});assert.match(m.errors.join('\n'),/requires pinned verified registry contract inputs/);assert.match(m.errors.join('\n'),/requires generated stubs to delegate authorization to provider services/)});test('ARCH-035-05 detects incomplete metadata and preserves immutability',()=>{class I extends D{compatibilityFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CONTRACT_SCHEMA_CLIENT_SERVER_GENERATION_ERROR_CODE);assert.throws(()=>complete(new D()).generatedClientFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
