import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{TOOLING_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE,ToolingOperationsGovernanceFutureEvolutionDescriptor as D,ToolingOperationsGovernanceFutureEvolutionProfile as P}from'../../src/cli-ide-local-tooling/index.js';const K=['ownershipFields','telemetryFields','supportFields','supplyChainFields','conformanceFields','deprecationFields','futureAssistanceFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI tooling operations',...x})};test('ARCH-035-10 exposes ownership telemetry support supply-chain conformance deprecation and future-assistance metadata',()=>{const d=new D();assert.ok(d.ownershipFields().includes('accountable-tooling-owner-maintainers-and-governing-body'));assert.ok(d.telemetryFields().includes('operation-tool-version-environment-class-outcome-latency-and-error-class-only'));assert.ok(d.supportFields().includes('issue-intake-with-redacted-support-bundle-attachment'));assert.ok(d.supplyChainFields().includes('software-bill-of-materials-published-per-release'));assert.ok(d.conformanceFields().includes('conformance-gate-before-release-and-plugin-listing'));assert.ok(d.deprecationFields().includes('deprecated-at-supported-until-and-removal-at-dates'));assert.ok(d.futureAssistanceFields().includes('ai-assistance-proposes-commands-fixes-and-plans-that-humans-accept'));assert.ok(d.invariants().includes('a-release-or-plugin-listing-never-bypasses-the-conformance-gate'))});test('ARCH-035-10 validates telemetry supply-chain conformance deprecation assistance and transport boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({toolingGrantsAuthority:true,telemetryLeaksSourceOrSecrets:true,unverifiedDependencyShipped:true,deprecationWithoutNotice:true,aiSilentlyActs:true,conformanceGateBypassed:true,supportBundleAutoUploaded:true,futureFeatureExpandsAuthority:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/telemetry containing source code arguments secrets or customer data/);assert.match(r.errors.join('\n'),/shipping or executing an unsigned unscanned or unverified dependency or release/);assert.match(r.errors.join('\n'),/a release or plugin listing bypassing the conformance gate/);assert.match(r.errors.join('\n'),/future feature expanding tooling authority beyond declared capabilities/);const m=d.validateProfile({profileName:'x',telemetryMinimized:false,supplyChainVerified:false,humanInTheLoop:false});assert.match(m.errors.join('\n'),/requires opt-in minimized retention-bounded telemetry free of source and secrets/);assert.match(m.errors.join('\n'),/requires proposal-only capability-bounded opt-in AI assistance/)});test('ARCH-035-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{supplyChainFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===TOOLING_OPERATIONS_GOVERNANCE_FUTURE_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).telemetryFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
