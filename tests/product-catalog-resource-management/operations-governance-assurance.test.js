import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CATALOG_OPERATIONS_CONTROLS,CATALOG_OPERATIONS_INVARIANTS,OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,OperationsGovernanceAssuranceDescriptor as D,OperationsGovernanceAssuranceProfile as P}from'../../src/product-catalog-resource-management/index.js';const K=['responsibilities','controls','recordFields','roles','lifecycleStages','measures','evidenceTypes','recovery','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Catalog Operations',...x})};test('ARCH-024-09 exposes operations controls measures evidence and invariants',()=>{const d=new D();assert.ok(d.responsibilities().includes('catalog-operating-model'));assert.ok(d.controls().includes(CATALOG_OPERATIONS_CONTROLS.DUTIES));assert.ok(d.measures().includes('reconciliation-drift'));assert.ok(d.invariants().includes(CATALOG_OPERATIONS_INVARIANTS.GOVERNED_AI))});test('ARCH-024-09 validates safeguards and rejects governance shortcuts',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({combinedDutiesUncontrolled:true,sensitiveMetadataUnprotected:true,tenantTestsOmitted:true,recoveryExercisesOmitted:true,unapprovedChange:true,fabricatedState:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/uncontrolled combined duties/);assert.match(x.errors.join('\n'),/unprotected sensitive metadata/);assert.match(x.errors.join('\n'),/omitted tenant tests/)});test('ARCH-024-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{controls(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.controls.push('x'),TypeError)});
