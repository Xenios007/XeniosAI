import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{RESOURCE_TYPES_LIFECYCLE_ERROR_CODE,ResourceTypesLifecycleDescriptor as D,ResourceTypesLifecycleProfile as P}from'../../src/product-catalog-resource-management/index.js';const K=['responsibilities','controls','recordFields','recovery','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Resources',...x})};test('ARCH-024-04 exposes resource responsibilities controls records recovery and invariants',()=>{const d=new D();assert.ok(d.responsibilities().includes('resource-groups-pools'));assert.ok(d.controls().includes('retirement-reconciliation'));assert.ok(d.recordFields().includes('solution-pack-compatibility-range'));assert.ok(d.recovery().includes('no-fabricated-offering-attribute-resource-eligibility-availability'));assert.ok(d.invariants().includes('no-projection-business-authority'))});test('ARCH-024-04 validates resource safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({sharedTablesOrDistributedTransactions:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/shared tables or distributed transactions/)});test('ARCH-024-04 detects incomplete metadata',()=>{class I extends D{controls(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===RESOURCE_TYPES_LIFECYCLE_ERROR_CODE);assert.throws(()=>complete(new D()).responsibilities.push('x'),TypeError)});
