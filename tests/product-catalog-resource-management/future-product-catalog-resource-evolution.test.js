import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{EVOLUTION_CONTROLS,EVOLUTION_INVARIANTS,FUTURE_PRODUCT_CATALOG_RESOURCE_EVOLUTION_ERROR_CODE,FutureProductCatalogResourceEvolutionDescriptor as D,FutureProductCatalogResourceEvolutionProfile as P}from'../../src/product-catalog-resource-management/index.js';const K=['evolutionPrinciples','evolutionHorizons','federatedPatterns','semanticInteroperability','aiStewardship','researchPromotionRetirement','recordFields','controls','recovery','observabilityFields','measures','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Future Catalog Evolution',...x})};test('ARCH-024-10 exposes evolution, federation, stewardship, recovery, and invariant metadata',()=>{const d=new D();assert.ok(d.evolutionHorizons().includes('federated-catalog-patterns'));assert.ok(d.aiStewardship().includes('evidence-confidence-alternatives-risk'));assert.ok(d.controls().includes(EVOLUTION_CONTROLS.ENDURING_AUTHORITY));assert.ok(d.invariants().includes(EVOLUTION_INVARIANTS.GOVERNED_AI))});test('ARCH-024-10 validates bounded evolution safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({unboundedExperimentation:true,projectionAuthority:true,fabricatedState:true,autonomousHighImpactActions:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/unbounded experimentation/);assert.match(x.errors.join('\n'),/projection authority/);assert.match(x.errors.join('\n'),/fabricated state/)});test('ARCH-024-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{evolutionHorizons(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_PRODUCT_CATALOG_RESOURCE_EVOLUTION_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.controls.push('x'),TypeError)});
