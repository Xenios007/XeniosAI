import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PRODUCT_CATALOG_RESOURCE_MANAGEMENT_OVERVIEW_ERROR_CODE,ProductCatalogResourceManagementOverviewDescriptor as D,ProductCatalogResourceManagementOverviewProfile as P}from'../../src/product-catalog-resource-management/index.js';const K=['capabilityResponsibilities','operatingModelControls','materialRecordFields','recoveryControls','telemetryFields','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Catalog',...x})};test('ARCH-024-01 exposes capability operating record recovery telemetry and invariant metadata',()=>{const d=new D();assert.ok(d.capabilityResponsibilities().includes('read-command-responsibility-separation'));assert.ok(d.operatingModelControls().includes('published-state-governed-new-version'));assert.ok(d.materialRecordFields().includes('solution-pack-compatibility-range'));assert.ok(d.recoveryControls().includes('no-fabricated-offering-attribute-resource-eligibility-availability'));assert.ok(d.telemetryFields().includes('correlation-identifier'));assert.ok(d.invariants().includes('no-client-agent-index-cache-integration-business-authority'))});test('ARCH-024-01 validates overview safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({sharedTablesOrDistributedTransactions:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/shared tables or distributed transactions/)});test('ARCH-024-01 detects incomplete metadata',()=>{class I extends D{recoveryControls(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PRODUCT_CATALOG_RESOURCE_MANAGEMENT_OVERVIEW_ERROR_CODE);assert.throws(()=>complete(new D()).telemetryFields.push('x'),TypeError)});
