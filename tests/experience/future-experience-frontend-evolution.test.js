import assert from'node:assert/strict';import test from'node:test';import{PlatformError,ServiceCollection}from'../../src/foundation/index.js';import{FUTURE_EXPERIENCE_FRONTEND_EVOLUTION_ERROR_CODE,FutureExperienceFrontendEvolutionDescriptor as D,FutureExperienceFrontendEvolutionProfile as P,addExperienceLayer}from'../../src/experience/index.js';const K=['evolutionHorizons','adaptationRequirements','multimodalControls','proactiveAssistanceControls','generativeUiConstraints','offlineProfileFields','enduringInvariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Future Experience',...x})};test('ARCH-021-10 exposes horizons adaptation multimodal proactive generated offline and invariant metadata',()=>{const d=new D();assert.ok(d.evolutionHorizons().includes('adaptive-ambient-experience'));assert.ok(d.adaptationRequirements().includes('reversible'));assert.ok(d.multimodalControls().includes('human-review'));assert.ok(d.proactiveAssistanceControls().includes('quiet-periods'));assert.ok(d.generativeUiConstraints().includes('allowlisted-components'));assert.ok(d.offlineProfileFields().includes('resynchronization'));assert.ok(d.enduringInvariants().includes('business-services-own-truth'))});test('ARCH-021-10 validates and rejects authority shortcuts',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({novelPresentationGrantsAuthority:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/granting authority/)});test('ARCH-021-10 detects incomplete metadata and is registered',()=>{class I extends D{enduringInvariants(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_EXPERIENCE_FRONTEND_EVOLUTION_ERROR_CODE);const d=addExperienceLayer(new ServiceCollection()).buildServiceProvider().getRequiredService('FutureExperienceFrontendEvolutionDescriptor');assert.equal(d.assertArchitecture().isValid,true);assert.throws(()=>complete(d).evolutionHorizons.push('x'),TypeError)});
