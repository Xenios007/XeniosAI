import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PORTAL_INFORMATION_ARCHITECTURE_DISCOVERY_ERROR_CODE,PortalInformationArchitectureDiscoveryDescriptor as D,PortalInformationArchitectureDiscoveryProfile as P}from'../../src/developer-experience-portal/index.js';const K=['navigationFields','searchFields','capabilityFields','guidanceFields','dashboardFields','artifactFlow','controls','failureRecovery','measures','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia portal discovery',...x})};test('ARCH-031-04 exposes navigation search capability guidance dashboard and publication flow',()=>{const d=new D();assert.ok(d.navigationFields().includes('localized-accessible-label-key'));assert.ok(d.searchFields().includes('source-freshness-and-index-lag'));assert.ok(d.capabilityFields().includes('deprecation-replacement-and-migration-reference'));assert.ok(d.guidanceFields().includes('safe-version-matched-fallback-content'));assert.ok(d.dashboardFields().includes('accessible-responsive-layout-profile'));assert.deepEqual(d.artifactFlow(),['new-or-changed-artifact','schema-compatibility-security-documentation-and-conformance-validation','owner-and-governance-approval','publish-version-and-notify-consumers','adoption-error-support-and-impact-monitoring','reject-restrict-supersede-or-roll-back'])});test('ARCH-031-04 validates discovery authority freshness accessibility publication and secret boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({searchGrantsPermission:true,restrictionsHidden:true,staleContentUnlabeled:true,inaccessibleDiscovery:true,fabricatedAvailability:true,unapprovedPublication:true,secretsInResults:true,personalizationExpandsAuthority:true,machineSelfPublishes:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/search from granting permission/);assert.match(r.errors.join('\n'),/unlabeled stale content/);assert.match(r.errors.join('\n'),/unapproved publication/);assert.match(r.errors.join('\n'),/machine self-publication/)});test('ARCH-031-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{searchFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PORTAL_INFORMATION_ARCHITECTURE_DISCOVERY_ERROR_CODE);assert.throws(()=>complete(new D()).capabilityFields.push('x'),TypeError)});
