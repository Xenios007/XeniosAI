import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{USAGE_QUOTAS_COST_OPERATIONAL_VISIBILITY_ERROR_CODE,UsageQuotasCostOperationalVisibilityDescriptor as D,UsageQuotasCostOperationalVisibilityProfile as P}from'../../src/developer-experience-portal/index.js';const K=['consumptionFields','quotaFields','costFields','healthFields','incidentFields','diagnosticFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia usage visibility',...x})};test('ARCH-031-06 exposes consumption quota cost health incident and diagnostic visibility',()=>{const d=new D();assert.ok(d.consumptionFields().includes('estimated-provisional-adjusted-or-final'));assert.ok(d.quotaFields().includes('normal-warning-exhausted-restricted-or-suspended'));assert.ok(d.costFields().includes('clearly-labeled-estimated-cost'));assert.ok(d.healthFields().includes('operational-degraded-partial-outage-major-outage-or-maintenance'));assert.ok(d.incidentFields().includes('investigating-identified-monitoring-or-resolved'));assert.ok(d.diagnosticFields().includes('secret-and-protected-data-redaction-result'))});test('ARCH-031-06 validates source cost quota tenant diagnostic and health boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({portalAuthorsUsage:true,costEstimatePresentedAsBill:true,quotaBypassed:true,stalenessHidden:true,crossTenantAggregation:true,secretsInDiagnostics:true,healthFabricated:true,automaticLimitExpansion:true,telemetryChangesPermissions:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/portal-authored usage/);assert.match(r.errors.join('\n'),/cost estimates presented as bills/);assert.match(r.errors.join('\n'),/secrets in diagnostics/);assert.match(r.errors.join('\n'),/fabricated health/)});test('ARCH-031-06 detects incomplete metadata and preserves immutability',()=>{class I extends D{quotaFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===USAGE_QUOTAS_COST_OPERATIONAL_VISIBILITY_ERROR_CODE);assert.throws(()=>complete(new D()).costFields.push('x'),TypeError)});
