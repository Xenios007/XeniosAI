import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PORTAL_INTEGRATION_EXTENSIBILITY_AUTOMATION_ERROR_CODE,PortalIntegrationExtensibilityAutomationDescriptor as D,PortalIntegrationExtensibilityAutomationProfile as P}from'../../src/developer-experience-portal/index.js';const K=['bffFields','eventFields','webhookFields','extensionFields','automationFields','externalSystemFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia portal integrations',...x})};test('ARCH-031-08 exposes BFF events webhooks extensions automation and external integration',()=>{const d=new D();assert.ok(d.bffFields().includes('purpose-bound-aggregated-projection'));assert.ok(d.eventFields().includes('minimum-completed-lifecycle-facts'));assert.ok(d.webhookFields().includes('timestamp-nonce-signature-and-replay-window'));assert.ok(d.extensionFields().includes('arch-019-package-version-reference'));assert.ok(d.automationFields().includes('arch-015-workflow-reference'));assert.ok(d.externalSystemFields().includes('versioned-identity-resource-and-status-mapping'))});test('ARCH-031-08 validates authority database credential webhook tenant and automation boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({directDatabaseAccess:true,ambientCredentials:true,extensionGrantsAuthority:true,unsignedWebhook:true,eventAckMeansCompletion:true,sharedTenantState:true,automationSelfApproves:true,protectedPayloadPropagated:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/direct database access/);assert.match(r.errors.join('\n'),/ambient credentials/);assert.match(r.errors.join('\n'),/unsigned webhooks/);assert.match(r.errors.join('\n'),/automation self-approval/)});test('ARCH-031-08 detects incomplete metadata and preserves immutability',()=>{class I extends D{webhookFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PORTAL_INTEGRATION_EXTENSIBILITY_AUTOMATION_ERROR_CODE);assert.throws(()=>complete(new D()).automationFields.push('x'),TypeError)});
