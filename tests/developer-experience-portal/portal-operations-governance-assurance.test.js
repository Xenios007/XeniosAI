import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PORTAL_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,PortalOperationsGovernanceAssuranceDescriptor as D,PortalOperationsGovernanceAssuranceProfile as P}from'../../src/developer-experience-portal/index.js';const K=['ownershipFields','analyticsFields','accessibilityFields','securityFields','performanceFields','incidentFields','contentFields','readinessFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia portal operations',...x})};test('ARCH-031-09 exposes ownership analytics accessibility security performance incident content and assurance contracts',()=>{const d=new D();assert.ok(d.ownershipFields().includes('accountable-portal-product-owner'));assert.ok(d.analyticsFields().includes('approved-measurement-purpose'));assert.ok(d.accessibilityFields().includes('automated-and-human-assistive-technology-testing'));assert.ok(d.securityFields().includes('provider-authoritative-resource-and-action-decision'));assert.ok(d.performanceFields().includes('explicit-stale-partial-read-only-and-unavailable-state'));assert.ok(d.incidentFields().includes('integrity-protected-incident-and-recovery-evidence'));assert.ok(d.contentFields().includes('preview-release-migration-deprecation-support-and-retirement'));assert.ok(d.readinessFields().includes('independent-assurance-owner'))});test('ARCH-031-09 validates authority privacy accessibility incident content machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({portalSelfAuthorizes:true,analyticsChangesPermissions:true,analyticsCopiesProtectedData:true,accessibilityExceptionSilent:true,securityControlsFailOpen:true,performanceHidesFailure:true,incidentFabricatesState:true,contentSelfPublishes:true,assuranceSelfApproved:true,machinePromotesChanges:true,protectedPayloadInTelemetry:true,tenantIsolated:false});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/portal self-authorization/);assert.match(r.errors.join('\n'),/protected data/);assert.match(r.errors.join('\n'),/silent accessibility/);assert.match(r.errors.join('\n'),/fabricating provider state/);assert.match(r.errors.join('\n'),/machine promotion/)});test('ARCH-031-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{contentFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PORTAL_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);assert.throws(()=>complete(new D()).assuranceEvidence.push('x'),TypeError)});
