import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FUTURE_DEVELOPER_EXPERIENCE_EVOLUTION_ERROR_CODE,FutureDeveloperExperienceEvolutionDescriptor as D,FutureDeveloperExperienceEvolutionProfile as P}from'../../src/developer-experience-portal/index.js';const K=['evolutionDirections','contextFields','contractFields','lifecycleStates','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia future developer journeys',...x})};test('ARCH-031-10 exposes personalized agent-assisted federated and self-service evolution',()=>{const d=new D();assert.deepEqual(d.evolutionDirections(),['personalized-developer-journeys','agent-assisted-developer-journeys','federated-developer-journeys','self-service-developer-journeys']);assert.ok(d.contextFields().includes('declared-purpose'));assert.ok(d.lifecycleStates().includes('deprecated'));assert.ok(d.controls().includes('machine-assistance-never-silently-approves-publishes-or-expands-permissions'))});test('ARCH-031-10 validates provider authority policy machine privacy and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({portalBecomesControlPlane:true,personalizationGrantsAuthority:true,agentSelfApproves:true,federationSharesTenantState:true,selfServiceBypassesPolicy:true,machinePublishesSilently:true,protectedPayloadInEvents:true,secretsInArtifacts:true,telemetryCopiesProtectedData:true,tenantIsolated:false});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/second control plane/);assert.match(r.errors.join('\n'),/agent self-approval/);assert.match(r.errors.join('\n'),/self-service bypassing policy/);assert.match(r.errors.join('\n'),/protected payloads/)});test('ARCH-031-10 detects incomplete evolution metadata and preserves immutability',()=>{class I extends D{evolutionDirections(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_DEVELOPER_EXPERIENCE_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).lifecycleStates.push('x'),TypeError)});
