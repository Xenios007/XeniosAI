import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CRM_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,CrmOperationsGovernanceAssuranceDescriptor as D,CrmOperationsGovernanceAssuranceProfile as P}from'../../src/customer-relationship-management/index.js';const K=['operatingRoles','serviceObjectives','telemetryFields','qualityIndicators','incidentClasses','governanceArtifacts','assuranceActivities','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Operations',...x})};test('ARCH-023-09 exposes roles objectives telemetry indicators incidents governance assurance and invariants',()=>{const d=new D();assert.ok(d.operatingRoles().includes('data-steward'));assert.ok(d.serviceObjectives().includes('withdrawal-suppression-propagation'));assert.ok(d.telemetryFields().includes('data-classification'));assert.ok(d.qualityIndicators().includes('merge-reversal-rate'));assert.ok(d.incidentClasses().includes('cross-tenant-disclosure'));assert.ok(d.governanceArtifacts().includes('context-projection-policies'));assert.ok(d.assuranceActivities().includes('architecture-conformance'));assert.ok(d.invariants().includes('no-inferred-authoritative-truth-under-pressure'))});test('ARCH-023-09 validates operating safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({operationalPressureConvertsInferenceToTruth:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/converting inference to truth/)});test('ARCH-023-09 detects incomplete metadata',()=>{class I extends D{assuranceActivities(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CRM_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);assert.throws(()=>complete(new D()).operatingRoles.push('x'),TypeError)});
