import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FUTURE_CUSTOMER_RELATIONSHIP_EVOLUTION_ERROR_CODE,FutureCustomerRelationshipEvolutionDescriptor as D,FutureCustomerRelationshipEvolutionProfile as P}from'../../src/customer-relationship-management/index.js';const K=['evolutionPrinciples','evolutionHorizons','aiStewardshipProposals','improvementStages','collaborationContractFields','promotionRequirements','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Evolution',...x})};test('ARCH-023-10 exposes evolution AI improvement collaboration promotion and invariant metadata',()=>{const d=new D();assert.ok(d.evolutionPrinciples().includes('governed-production-promotion'));assert.ok(d.evolutionHorizons().includes('adaptive-relationships'));assert.ok(d.aiStewardshipProposals().includes('duplicate-candidates'));assert.ok(d.improvementStages().includes('approval-canary-monitoring-rollback'));assert.ok(d.collaborationContractFields().includes('tenant-mapping'));assert.ok(d.promotionRequirements().includes('retirement-plan'));assert.ok(d.invariants().includes('predictions-not-facts-permissions'))});test('ARCH-023-10 validates evolution safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({customerOrModelOutputDirectlyChangesProductionBehavior:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/direct production behavior changes/)});test('ARCH-023-10 detects incomplete metadata',()=>{class I extends D{promotionRequirements(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_CUSTOMER_RELATIONSHIP_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).evolutionHorizons.push('x'),TypeError)});
