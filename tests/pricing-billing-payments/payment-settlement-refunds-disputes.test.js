import assert from'node:assert/strict';import test from'node:test';import{PaymentSettlementRefundsDisputesDescriptor as D,PaymentSettlementRefundsDisputesProfile as P}from'../../src/pricing-billing-payments/index.js';test('ARCH-026-07 validates payment settlement refund and dispute safeguards',()=>{const d=new D(),p=new P({profileName:'Payments',paymentIntentFields:d.paymentIntentFields(),settlementFields:d.settlementFields(),refundFields:d.refundFields(),disputeFields:d.disputeFields(),financialStateAuthoritative:true,providerOwnsValidation:true,settlementReconciled:true,refundsGoverned:true,disputesEvidenceBased:true});assert.equal(d.validateProfile(p).isValid,true);assert.ok(d.disputeFields().includes('dispute-evidence'));assert.equal(d.validateProfile({silentMoneyChange:true}).isValid,false)});
