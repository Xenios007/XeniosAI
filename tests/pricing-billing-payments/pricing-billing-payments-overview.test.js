import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FINANCIAL_INVARIANTS,PRICING_BILLING_PAYMENTS_OVERVIEW_ERROR_CODE,PricingBillingPaymentsOverviewDescriptor as D,PricingBillingPaymentsOverviewProfile as P}from'../../src/pricing-billing-payments/index.js';const K=['responsibilities','authorities','contractFields','aggregateFields','lifecycleStates','governanceControls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Pricing Billing Payments Overview',...x})};test('ARCH-026-01 exposes financial authority governance recovery and assurance metadata',()=>{const d=new D();assert.ok(d.authorities().includes('calculated-charges'));assert.ok(d.lifecycleStates().includes('restricted'));assert.ok(d.governanceControls().includes('default-deny'));assert.ok(d.invariants().includes(FINANCIAL_INVARIANTS.AI_EVALUATION))});test('ARCH-026-01 validates financial authority boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({silentMoneyChange:true,generatedCompletionAccepted:true,extensionsWeakenControls:true,aiPromotionOutsideArch029:true,pricingOrPaymentOwnedElsewhere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/silent money changes/);assert.match(x.errors.join('\n'),/generated or optimistic completion/);assert.match(x.errors.join('\n'),/extensions weakening controls/)});test('ARCH-026-01 detects incomplete metadata and preserves immutability',()=>{class I extends D{governanceControls(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PRICING_BILLING_PAYMENTS_OVERVIEW_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.governanceControls.push('x'),TypeError)});
