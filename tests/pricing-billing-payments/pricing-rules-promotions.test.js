import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PRICING_RULES_PROMOTIONS_ERROR_CODE,RULES_INVARIANTS,PricingRulesPromotionsDescriptor as D,PricingRulesPromotionsProfile as P}from'../../src/pricing-billing-payments/index.js';const K=['responsibilities','ruleFields','eligibilityFields','stackingFields','overrideFields','promotionFields','approvalFields','explanationFields','commandFields','eventFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Pricing Rules Promotions',...x})};test('ARCH-026-03 exposes rules eligibility stacking overrides approvals and explanations',()=>{const d=new D();assert.ok(d.ruleFields().includes('conditions'));assert.ok(d.eligibilityFields().includes('eligibility-evidence'));assert.ok(d.approvalFields().includes('approval-limit'));assert.ok(d.invariants().includes(RULES_INVARIANTS.DETERMINISTIC))});test('ARCH-026-03 validates deterministic pricing safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({nondeterministicRules:true,unapprovedOverride:true,unboundedStacking:true,approvalLimitBypassed:true,ungroundedExplanation:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/nondeterministic rules/);assert.match(x.errors.join('\n'),/unapproved overrides/);assert.match(x.errors.join('\n'),/unbounded stacking/)});test('ARCH-026-03 detects incomplete metadata and preserves immutability',()=>{class I extends D{ruleFields(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PRICING_RULES_PROMOTIONS_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.ruleFields.push('x'),TypeError)});
