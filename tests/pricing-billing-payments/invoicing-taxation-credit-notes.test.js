import assert from'node:assert/strict';import test from'node:test';import{InvoicingTaxationCreditNotesDescriptor as D,InvoicingTaxationCreditNotesProfile as P}from'../../src/pricing-billing-payments/index.js';test('ARCH-026-06 validates invoice tax receivable credit and integrity metadata',()=>{const d=new D(),p=new P({profileName:'Invoice',invoiceFields:d.invoiceFields(),taxFields:d.taxFields(),receivableFields:d.receivableFields(),creditFields:d.creditFields(),financialStateAuthoritative:true,providerOwnsValidation:true,documentIntegrityPreserved:true,taxReferencesAuthoritative:true,creditsGoverned:true});assert.equal(d.validateProfile(p).isValid,true);assert.ok(d.invoiceFields().includes('document-integrity-evidence'));assert.equal(d.validateProfile({silentMoneyChange:true}).isValid,false)});
