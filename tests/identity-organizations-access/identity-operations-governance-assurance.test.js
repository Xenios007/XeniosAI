import assert from'node:assert/strict';import test from'node:test';import{PlatformError,ServiceCollection}from'../../src/foundation/index.js';import{IDENTITY_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,IdentityOperationsGovernanceAssuranceDescriptor as D,IdentityOperationsGovernanceAssuranceProfile as P,addIdentityOrganizationsAccess}from'../../src/identity-organizations-access/index.js';const K=['operatingResponsibilities','serviceIndicators','operationalReadinessElements','incidentTypes','governanceArtifacts','assuranceActivities'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Operations',...x})};test('ARCH-022-09 exposes operating indicators readiness incidents governance and assurance metadata',()=>{const d=new D();assert.ok(d.operatingResponsibilities().includes('identity-operations'));assert.ok(d.serviceIndicators().includes('revocation-effectiveness'));assert.ok(d.operationalReadinessElements().includes('provider-exit'));assert.ok(d.incidentTypes().includes('agent-delegation-violation'));assert.ok(d.governanceArtifacts().includes('machine-agent-inventory'));assert.ok(d.assuranceActivities().includes('revocation-drills'))});test('ARCH-022-09 validates and rejects metrics leakage',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({credentialsProofingMaterialExposedInMetrics:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/proofing material in metrics/)});test('ARCH-022-09 detects incomplete metadata and is registered',()=>{class I extends D{incidentTypes(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===IDENTITY_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);const d=addIdentityOrganizationsAccess(new ServiceCollection()).buildServiceProvider().getRequiredService('IdentityOperationsGovernanceAssuranceDescriptor');assert.equal(d.assertArchitecture().isValid,true);assert.throws(()=>complete(d).serviceIndicators.push('x'),TypeError)});
