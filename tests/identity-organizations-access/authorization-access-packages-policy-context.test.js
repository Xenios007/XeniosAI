import assert from'node:assert/strict';import test from'node:test';import{PlatformError,ServiceCollection}from'../../src/foundation/index.js';import{AUTHORIZATION_ACCESS_PACKAGES_POLICY_CONTEXT_ERROR_CODE,AuthorizationAccessPackagesPolicyContextDescriptor as D,AuthorizationAccessPackagesPolicyContextProfile as P,addIdentityOrganizationsAccess}from'../../src/identity-organizations-access/index.js';const K=['authorizationContexts','accessRequestFields','accessPackageFields','policyContextFacts','providerBoundaries','reviewFactors','decisionEvidenceFields'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Authorization',...x})};test('ARCH-022-05 exposes context request package provider review and evidence metadata',()=>{const d=new D();assert.ok(d.authorizationContexts().includes('capability-grants'));assert.ok(d.accessRequestFields().includes('conflicting-duties'));assert.ok(d.accessPackageFields().includes('review-frequency'));assert.ok(d.policyContextFacts().includes('consent-ownership-lifecycle'));assert.ok(d.providerBoundaries().includes('tools-workflows-knowledge-memory'));assert.ok(d.reviewFactors().includes('non-response-expiry-escalation'));assert.ok(d.decisionEvidenceFields().includes('minimized-sensitive-values'))});test('ARCH-022-05 validates and rejects role shortcut',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({rolesGrantEverything:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/granting everything/)});test('ARCH-022-05 detects incomplete metadata and is registered',()=>{class I extends D{reviewFactors(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===AUTHORIZATION_ACCESS_PACKAGES_POLICY_CONTEXT_ERROR_CODE);const d=addIdentityOrganizationsAccess(new ServiceCollection()).buildServiceProvider().getRequiredService('AuthorizationAccessPackagesPolicyContextDescriptor');assert.equal(d.assertArchitecture().isValid,true);assert.throws(()=>complete(d).authorizationContexts.push('x'),TypeError)});
