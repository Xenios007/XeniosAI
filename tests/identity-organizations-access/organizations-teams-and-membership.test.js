import assert from'node:assert/strict';import test from'node:test';import{PlatformError,ServiceCollection}from'../../src/foundation/index.js';import{ORGANIZATIONS_TEAMS_AND_MEMBERSHIP_ERROR_CODE,OrganizationsTeamsAndMembershipDescriptor as D,OrganizationsTeamsAndMembershipProfile as P,addIdentityOrganizationsAccess}from'../../src/identity-organizations-access/index.js';const K=['organizationTypes','unitTeamFields','membershipFields','membershipStates','partnerAccessFactors','groupControls','organizationChangeReviews'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Organizations',...x})};test('ARCH-022-03 exposes organization team membership partner group and lifecycle metadata',()=>{const d=new D();assert.ok(d.organizationTypes().includes('temporary-collaboration-organization'));assert.ok(d.unitTeamFields().includes('external-collaboration-policy'));assert.ok(d.membershipFields().includes('conditions-sod'));assert.ok(d.membershipStates().includes('pending-verification'));assert.ok(d.partnerAccessFactors().includes('exit-obligations'));assert.ok(d.groupControls().includes('direct-high-risk-assignment-approval'));assert.ok(d.organizationChangeReviews().includes('ownership-orphan-prevention'))});test('ARCH-022-03 validates and rejects membership shortcuts',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({changeDoesNotLeaveOrphanedPrivilegedAccess:true});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/orphaned privileged access/)});test('ARCH-022-03 detects incomplete metadata and is registered',()=>{class I extends D{membershipStates(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===ORGANIZATIONS_TEAMS_AND_MEMBERSHIP_ERROR_CODE);const d=addIdentityOrganizationsAccess(new ServiceCollection()).buildServiceProvider().getRequiredService('OrganizationsTeamsAndMembershipDescriptor');assert.equal(d.assertArchitecture().isValid,true);assert.throws(()=>complete(d).organizationTypes.push('x'),TypeError)});
