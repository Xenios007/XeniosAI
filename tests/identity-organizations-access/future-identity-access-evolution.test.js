import assert from'node:assert/strict';import test from'node:test';import{PlatformError,ServiceCollection}from'../../src/foundation/index.js';import{FUTURE_IDENTITY_ACCESS_EVOLUTION_ERROR_CODE,FutureIdentityAccessEvolutionDescriptor as D,FutureIdentityAccessEvolutionProfile as P,addIdentityOrganizationsAccess}from'../../src/identity-organizations-access/index.js';const K=['evolutionHorizons','adaptiveAccessDimensions','portableClaimValidations','federatedAgentWorkloadRequirements','decentralizedAdministrationControls','identityIntelligenceAssistance','researchPromotionRequirements','enduringInvariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Evolution',...x})};test('ARCH-022-10 exposes future access, portable claims, federation, intelligence, research, and invariant metadata',()=>{const d=new D();assert.ok(d.evolutionHorizons().includes('privacy-preserving-federation'));assert.ok(d.adaptiveAccessDimensions().includes('transaction-limits'));assert.ok(d.portableClaimValidations().includes('minimum-disclosure'));assert.ok(d.federatedAgentWorkloadRequirements().includes('chain-of-delegation'));assert.ok(d.decentralizedAdministrationControls().includes('automatic-expiry'));assert.ok(d.identityIntelligenceAssistance().includes('remediation-proposal'));assert.ok(d.researchPromotionRequirements().includes('bias-evaluation'));assert.ok(d.enduringInvariants().includes('identity-is-not-authority'))});test('ARCH-022-10 validates evolved access safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const v=d.validateProfile({riskScoringIsDecisionContextNotIdentityOrPermanentReputation:false});assert.equal(v.isValid,false);assert.match(v.errors.join('\n'),/must have a name/);assert.match(v.errors.join('\n'),/risk scoring to remain decision context/)});test('ARCH-022-10 detects incomplete metadata and is registered',()=>{class I extends D{enduringInvariants(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_IDENTITY_ACCESS_EVOLUTION_ERROR_CODE);const d=addIdentityOrganizationsAccess(new ServiceCollection()).buildServiceProvider().getRequiredService('FutureIdentityAccessEvolutionDescriptor');assert.equal(d.assertArchitecture().isValid,true);assert.throws(()=>complete(d).evolutionHorizons.push('x'),TypeError)});
