import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PARTNER_PROGRAM_MODELS_TIERS_LIFECYCLE_ERROR_CODE,PartnerProgramModelsTiersLifecycleDescriptor as D,PartnerProgramModelsTiersLifecycleProfile as P}from'../../src/partner-community-ecosystem-federation/index.js';const K=['partnerModelFields','tierFields','benefitFields','obligationFields','reviewFields','exitFields','agreementFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI partner program',...x})};test('ARCH-040-02 exposes model tier benefit obligation review exit and agreement metadata',()=>{const d=new D();assert.ok(d.partnerModelFields().includes('a-partner-may-hold-more-than-one-model-with-distinct-terms'));assert.ok(d.tierFields().includes('a-tier-never-grants-platform-or-tenant-runtime-authority'));assert.ok(d.benefitFields().includes('benefits-applied-consistently-within-a-tier'));assert.ok(d.obligationFields().includes('partner-support-and-security-response-obligations'));assert.ok(d.reviewFields().includes('recertification-of-competency-and-obligations'));assert.ok(d.exitFields().includes('exit-never-holds-a-customer-engagement-or-data-hostage'));assert.ok(d.agreementFields().includes('explicit-recorded-acceptance-by-an-authorized-signatory'));assert.ok(d.invariants().includes('a-tier-demotion-is-never-applied-without-advance-notice-and-a-remediation-window'))});test('ARCH-040-02 validates authority benefits obligations demotion exit and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({tierGrantsRuntimeAuthority:true,benefitsUndisclosed:true,obligationsUnenforced:true,demotionWithoutNotice:true,exitHoldsCustomerHostage:true,aiSilentlyChangesTier:true,secretsInPartnerRecord:true,partnerStatusOverridesProviderAuthz:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a partner tier or status granting platform or tenant runtime authority/);assert.match(r.errors.join('\n'),/a tier demotion without advance notice and a remediation window/);assert.match(r.errors.join('\n'),/AI assistance silently changing a tier benefit or obligation/);assert.match(r.errors.join('\n'),/partner status overriding provider or external authorization/);const m=d.validateProfile({profileName:'x',tiersObjective:false,obligationsTracked:false,agreementAccepted:false});assert.match(m.errors.join('\n'),/requires partner tiers with objective published criteria and a demotion notice and remediation window/);assert.match(m.errors.join('\n'),/requires an authorized signatory explicit acceptance of the versioned partner agreement/)});test('ARCH-040-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{benefitFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PARTNER_PROGRAM_MODELS_TIERS_LIFECYCLE_ERROR_CODE);assert.throws(()=>complete(new D()).tierFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
