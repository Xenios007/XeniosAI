import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{ECOSYSTEM_COLLABORATION_SUPPORT_CONFLICT_RESILIENCE_ERROR_CODE,EcosystemCollaborationSupportConflictResilienceDescriptor as D,EcosystemCollaborationSupportConflictResilienceProfile as P}from'../../src/partner-community-ecosystem-federation/index.js';const K=['sharedOperationsFields','supportBoundaryFields','incidentCoordinationFields','disputeResolutionFields','vulnerabilityDisclosureFields','continuityFields','resilienceRecoveryFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI ecosystem collaboration',...x})};test('ARCH-040-08 exposes operations support incident dispute vulnerability continuity and recovery metadata',()=>{const d=new D();assert.ok(d.sharedOperationsFields().includes('coordination-of-a-change-that-affects-more-than-one-party'));assert.ok(d.supportBoundaryFields().includes('no-support-scenario-left-without-an-accountable-owner'));assert.ok(d.incidentCoordinationFields().includes('a-blameless-joint-postmortem-with-shared-action-items'));assert.ok(d.disputeResolutionFields().includes('a-neutral-reviewer-independent-of-the-parties-in-dispute'));assert.ok(d.vulnerabilityDisclosureFields().includes('a-coordinated-disclosure-timeline-across-affected-parties'));assert.ok(d.continuityFields().includes('source-or-data-escrow-for-a-critical-single-provider-dependency'));assert.ok(d.resilienceRecoveryFields().includes('degrade-to-trusted-cached-metadata-with-freshness-labels-on-federation-loss'));assert.ok(d.invariants().includes('a-support-or-incident-scenario-is-never-left-without-an-accountable-owner'))});test('ARCH-040-08 validates orphan incident dispute disclosure continuity and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({supportGapUnowned:true,incidentHandledInSilence:true,disputeDecidedWithoutAppeal:true,vulnerabilityDisclosedUncoordinated:true,continuityDependsOnSingleParty:true,aiSilentlyResolvesDispute:true,secretsInIncidentRecord:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/leaving a support or incident scenario without an accountable owner/);assert.match(r.errors.join('\n'),/handling a cross-party incident in silence toward affected tenants and customers/);assert.match(r.errors.join('\n'),/disclosing a vulnerability outside the coordinated timeline or suppressing it/);assert.match(r.errors.join('\n'),/AI silently resolving a dispute closing an incident or suppressing a disclosure/);const m=d.validateProfile({profileName:'x',incidentsCoordinated:false,continuityPlanned:false,disputesResolvedFairly:false});assert.match(m.errors.join('\n'),/requires incidents coordinated with an incident commander status updates and a blameless joint postmortem/);assert.match(m.errors.join('\n'),/requires continuity planned with dependency mapping recovery objectives fallback modes and periodic exercises/)});test('ARCH-040-08 detects incomplete metadata and preserves immutability',()=>{class I extends D{disputeResolutionFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===ECOSYSTEM_COLLABORATION_SUPPORT_CONFLICT_RESILIENCE_ERROR_CODE);assert.throws(()=>complete(new D()).continuityFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
