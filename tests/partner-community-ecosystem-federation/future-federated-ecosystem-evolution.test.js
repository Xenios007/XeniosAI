import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FUTURE_FEDERATED_ECOSYSTEM_EVOLUTION_ERROR_CODE,FutureFederatedEcosystemEvolutionDescriptor as D,FutureFederatedEcosystemEvolutionProfile as P}from'../../src/partner-community-ecosystem-federation/index.js';const K=['evolutionDirections','portableTrustFields','decentralizedCatalogFields','interoperableCommunityFields','durableStewardshipFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI federated ecosystem evolution',...x})};test('ARCH-040-10 exposes evolution-direction portable-trust decentralized-catalog community and stewardship metadata',()=>{const d=new D();assert.ok(d.evolutionDirections().includes('durable-ecosystem-stewardship-with-succession-escrow-and-no-single-point-of-capture'));assert.ok(d.portableTrustFields().includes('local-policy-evaluation-of-a-portable-claim-before-any-access'));assert.ok(d.decentralizedCatalogFields().includes('discovery-federation-that-never-transfers-installation-or-runtime-authority'));assert.ok(d.interoperableCommunityFields().includes('no-stranding-of-customers-or-contributors-when-a-community-moves-or-splits'));assert.ok(d.durableStewardshipFields().includes('safeguards-against-capture-of-the-ecosystem-by-a-single-party'));assert.ok(d.invariants().includes('a-trust-claim-a-contribution-or-a-contributor-identity-is-never-locked-to-a-single-platform'))});test('ARCH-040-10 validates authority local-policy stranding capture and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({portableTrustGrantsAuthority:true,decentralizedCatalogBypassesLocalPolicy:true,communityInteropStrandsCustomers:true,stewardshipCapturedByOneParty:true,aiSilentlyEvolvesEcosystem:true,secretsInEvolutionArtifacts:true,crossTenantFederatedState:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a decentralized catalog entry bypassing local policy evaluation or runtime authorization/);assert.match(r.errors.join('\n'),/a community move or split stranding customers or contributors or stripping attribution/);assert.match(r.errors.join('\n'),/ecosystem stewardship becoming a single point of capture without succession and escrow/);assert.match(r.errors.join('\n'),/AI assistance silently evolving the ecosystem or changing its governance/);const m=d.validateProfile({profileName:'x',trustPortable:false,stewardshipDurable:false,platformAuthorityPreserved:false});assert.match(m.errors.join('\n'),/requires trust claims contributions and identity to remain portable and free of lock-in/);assert.match(m.errors.join('\n'),/requires durable ecosystem stewardship with succession escrow and no single point of capture/)});test('ARCH-040-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{portableTrustFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_FEDERATED_ECOSYSTEM_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).evolutionDirections.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
