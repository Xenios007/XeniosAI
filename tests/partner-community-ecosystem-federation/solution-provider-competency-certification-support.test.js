import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{SOLUTION_PROVIDER_COMPETENCY_CERTIFICATION_SUPPORT_ERROR_CODE,SolutionProviderCompetencyCertificationSupportDescriptor as D,SolutionProviderCompetencyCertificationSupportProfile as P}from'../../src/partner-community-ecosystem-federation/index.js';const K=['competencyFields','trainingFields','assessmentFields','certificationFields','deliveryStandardsFields','supportFields','renewalSuspensionFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI provider certification',...x})};test('ARCH-040-04 exposes competency training assessment certification delivery support and renewal metadata',()=>{const d=new D();assert.ok(d.competencyFields().includes('the-evidence-a-provider-must-supply-to-claim-a-competency'));assert.ok(d.trainingFields().includes('training-refresh-on-a-breaking-or-major-platform-change'));assert.ok(d.assessmentFields().includes('objective-scoring-independent-of-commercial-relationship'));assert.ok(d.certificationFields().includes('issuance-only-after-a-passed-assessment-and-verified-evidence'));assert.ok(d.deliveryStandardsFields().includes('a-minimum-certified-staff-ratio-on-an-engagement'));assert.ok(d.supportFields().includes('severity-based-response-and-restoration-targets'));assert.ok(d.renewalSuspensionFields().includes('protection-and-reassignment-of-customers-of-a-suspended-provider'));assert.ok(d.invariants().includes('certification-status-never-creates-unrestricted-platform-or-tenant-authority'))});test('ARCH-040-04 validates assessment evidence delivery support suspension and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({certificationWithoutAssessment:true,competencyClaimedWithoutEvidence:true,deliveryBelowStandard:true,supportObligationUnmet:true,suspensionWithoutProcess:true,aiSilentlyCertifiesProvider:true,certificationGrantsPlatformAuthority:true,secretsInCertificationRecord:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/issuing a certification without a passed assessment and verified evidence/);assert.match(r.errors.join('\n'),/delivering an engagement below the published standard or certified staff ratio/);assert.match(r.errors.join('\n'),/AI silently certifying suspending or reinstating a provider/);assert.match(r.errors.join('\n'),/certification status creating unrestricted platform or tenant authority/);const m=d.validateProfile({profileName:'x',trainingAvailable:false,supportObligationsMet:false,deliveryStandardsEnforced:false});assert.match(m.errors.join('\n'),/requires available versioned accessible training refreshed on a major change/);assert.match(m.errors.join('\n'),/requires defined support tiers response targets and escalation paths that are met/)});test('ARCH-040-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{certificationFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===SOLUTION_PROVIDER_COMPETENCY_CERTIFICATION_SUPPORT_ERROR_CODE);assert.throws(()=>complete(new D()).assessmentFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
