import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{PARTNER_COMMUNITY_ECOSYSTEM_FEDERATION_OVERVIEW_ERROR_CODE,PartnerCommunityEcosystemFederationOverviewDescriptor as D,PartnerCommunityEcosystemFederationOverviewProfile as P,ECOSYSTEM_ACTORS}from'../../src/partner-community-ecosystem-federation/index.js';const K=['ecosystemActors','participationModels','trustModelFields','federationFields','governanceFields','sustainabilityFields','qualityAttributes','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI ecosystem federation',...x})};test('ARCH-040-01 exposes actors participation trust federation governance sustainability and quality metadata',()=>{const d=new D();assert.ok(d.ecosystemActors().includes(ECOSYSTEM_ACTORS.COMMUNITY));assert.ok(d.participationModels().includes('certified-solution-provider-competency'));assert.ok(d.trustModelFields().includes('ecosystem-membership-or-tier-never-creates-platform-or-tenant-authority'));assert.ok(d.federationFields().includes('provenance-and-freshness-preserving-metadata-exchange'));assert.ok(d.governanceFields().includes('a-neutral-cross-organization-dispute-resolution-process'));assert.ok(d.sustainabilityFields().includes('measures-that-preserve-small-and-new-participant-viability'));assert.ok(d.qualityAttributes().includes('customers-and-tenants-can-remove-migrate-correct-appeal-and-exit'));assert.ok(d.invariants().includes('ecosystem-status-never-creates-unrestricted-platform-or-tenant-authority'))});test('ARCH-040-01 validates authority runtime provenance exit provider and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({ecosystemStatusGrantsAuthority:true,runtimeGrantAmbient:true,provenanceLostInFederation:true,participantCannotExit:true,aiSilentlyApprovesOrCertifies:true,federationBypassesProviderAuthz:true,secretsInEcosystemMetadata:true,sharedTenantEcosystemState:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/ecosystem status membership or tier creating platform or tenant authority/);assert.match(r.errors.join('\n'),/a federated or partner relationship bypassing provider or external authorization/);assert.match(r.errors.join('\n'),/AI silently approving ranking certifying suspending or settling/);assert.match(r.errors.join('\n'),/a customer tenant or participant cannot remove migrate correct or exit/);const m=d.validateProfile({profileName:'x',provenancePreserved:false,exitSupported:false,runtimeGrantsExplicit:false});assert.match(m.errors.join('\n'),/requires provenance and accountable ownership to survive federation and redistribution/);assert.match(m.errors.join('\n'),/requires runtime grants to remain explicit local least-privilege and revocable/)});test('ARCH-040-01 detects incomplete metadata and preserves immutability',()=>{class I extends D{trustModelFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===PARTNER_COMMUNITY_ECOSYSTEM_FEDERATION_OVERVIEW_ERROR_CODE);assert.throws(()=>complete(new D()).participationModels.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
