import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{ECOSYSTEM_OPERATIONS_GOVERNANCE_METRICS_ASSURANCE_ERROR_CODE,EcosystemOperationsGovernanceMetricsAssuranceDescriptor as D,EcosystemOperationsGovernanceMetricsAssuranceProfile as P}from'../../src/partner-community-ecosystem-federation/index.js';const K=['councilGovernanceFields','ownershipDecisionRightsFields','ecosystemHealthMetricFields','fairnessFields','riskManagementFields','governanceEvidenceAuditFields','continuousImprovementFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI ecosystem governance',...x})};test('ARCH-040-09 exposes council decision-rights health fairness risk evidence and improvement metadata',()=>{const d=new D();assert.ok(d.councilGovernanceFields().includes('balanced-representation-across-platform-partners-publishers-and-customers'));assert.ok(d.ownershipDecisionRightsFields().includes('decisions-reserved-to-the-platform-operator-for-safety-and-ownership'));assert.ok(d.ecosystemHealthMetricFields().includes('published-metric-definitions-and-calculation-methods'));assert.ok(d.fairnessFields().includes('disclosed-limits-on-platform-self-preferencing'));assert.ok(d.riskManagementFields().includes('concentration-and-single-provider-dependency-risk-tracking'));assert.ok(d.governanceEvidenceAuditFields().includes('periodic-independent-audit-of-ecosystem-governance-and-controls'));assert.ok(d.continuousImprovementFields().includes('a-prioritized-improvement-backlog-with-owners'));assert.ok(d.invariants().includes('ecosystem-governance-status-never-creates-unrestricted-platform-or-tenant-authority'))});test('ARCH-040-09 validates representation decision-record metric fairness concentration and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({governanceWithoutRepresentation:true,decisionWithoutRecord:true,metricGamedOrHidden:true,fairnessComplaintIgnored:true,concentrationRiskUnmanaged:true,aiSilentlyGovernsEcosystem:true,secretsInGovernanceRecord:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a binding ecosystem decision made without the chartered body and its representation rules/);assert.match(r.errors.join('\n'),/an ecosystem metric being gamed hidden or published without its definition/);assert.match(r.errors.join('\n'),/leaving a material concentration risk untracked and unmitigated/);assert.match(r.errors.join('\n'),/AI silently setting policy deciding a case or governing the ecosystem/);const m=d.validateProfile({profileName:'x',healthMeasured:false,risksManaged:false,fairnessMonitored:false});assert.match(m.errors.join('\n'),/requires ecosystem health measured with published metric definitions and calculation methods/);assert.match(m.errors.join('\n'),/requires fairness monitored with transparent criteria a complaint channel bias review and remedies/)});test('ARCH-040-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{fairnessFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===ECOSYSTEM_OPERATIONS_GOVERNANCE_METRICS_ASSURANCE_ERROR_CODE);assert.throws(()=>complete(new D()).riskManagementFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
