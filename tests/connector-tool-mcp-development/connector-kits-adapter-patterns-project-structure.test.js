import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CONNECTOR_KITS_ADAPTER_PATTERNS_PROJECT_STRUCTURE_ERROR_CODE,ConnectorKitsAdapterPatternsProjectStructureDescriptor as D,ConnectorKitsAdapterPatternsProjectStructureProfile as P}from'../../src/connector-tool-mcp-development/index.js';const K=['templateFields','portsAdaptersFields','manifestFields','configurationFields','dependencyFields','packagingFields','ownershipFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI connector kits',...x})};test('ARCH-038-02 exposes template ports-adapters manifest config dependency packaging and ownership metadata',()=>{const d=new D();assert.ok(d.templateFields().includes('scaffold-of-ports-adapters-manifest-config-and-tests'));assert.ok(d.portsAdaptersFields().includes('no-external-system-detail-leaks-into-the-domain-port'));assert.ok(d.manifestFields().includes('requested-scopes-network-egress-and-data-classes'));assert.ok(d.configurationFields().includes('secrets-referenced-by-handle-and-injected-at-runtime'));assert.ok(d.dependencyFields().includes('exact-version-pinning-and-a-committed-lockfile'));assert.ok(d.packagingFields().includes('the-manifest-is-embedded-and-integrity-protected'));assert.ok(d.ownershipFields().includes('ownership-transfer-and-absentee-reassignment'));assert.ok(d.invariants().includes('business-and-domain-logic-never-lives-in-an-external-system-adapter'))});test('ARCH-038-02 validates adapter-logic manifest secrets dependency packaging and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({businessLogicInAdapter:true,manifestOmitsCapabilities:true,secretsInConfigFile:true,unpinnedDependency:true,unsignedPackage:true,aiSilentlyModifiesManifest:true,connectorWithoutOwner:true,reproducibilityBroken:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/business or domain logic living in an external system adapter/);assert.match(r.errors.join('\n'),/secret material in a configuration file or package artifact/);assert.match(r.errors.join('\n'),/AI assistance silently modifying a manifest a permission set or a dependency pin/);assert.match(r.errors.join('\n'),/a connector existing without an accountable owner and support path/);const m=d.validateProfile({profileName:'x',manifestComplete:false,dependenciesPinned:false,packageSigned:false});assert.match(m.errors.join('\n'),/requires a manifest that completely declares identity capabilities permissions and contract versions/);assert.match(m.errors.join('\n'),/requires reproducible signed and manifest-embedded packages/)});test('ARCH-038-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{manifestFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CONNECTOR_KITS_ADAPTER_PATTERNS_PROJECT_STRUCTURE_ERROR_CODE);assert.throws(()=>complete(new D()).portsAdaptersFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
