import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_PRODUCT_MODEL_OWNERSHIP_AUDIENCES_ERROR_CODE,ApiProductModelOwnershipAudiencesDescriptor as D,ApiProductModelOwnershipAudiencesProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['identityFields','ownerFields','consumerFields','audienceFields','valueFields','includedContractFields','supportFields','policyFields','lifecycleFields','controls','failureRecovery','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API product model',...x})};test('ARCH-033-02 exposes identity owners consumers audiences value contracts support policy and lifecycle',()=>{const d=new D();assert.ok(d.identityFields().includes('stable-api-product-identifier'));assert.ok(d.ownerFields().includes('authoritative-domain-service-owner-references'));assert.ok(d.consumerFields().includes('internal-tenant-partner-solution-application-automation-or-agent'));assert.ok(d.audienceFields().includes('identity-organization-tenant-environment-risk-and-policy-criteria'));assert.ok(d.valueFields().includes('discovery-access-first-call-adoption-retention-and-outcome-measures'));assert.ok(d.includedContractFields().includes('arch-017-contract-and-version-reference'));assert.ok(d.supportFields().includes('preview-active-deprecated-and-retired-support-commitments'));assert.ok(d.policyFields().includes('authoritative-policy-service-and-version-reference'))});test('ARCH-033-02 validates owner audience state semantics registration support machine secret and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({ownerGrantsProviderAuthority:true,audienceMeansPermission:true,productOwnsBusinessState:true,contractRedefinesSemantics:true,unregisteredContract:true,supportBypassesPolicy:true,machineAssignsOwner:true,sharedTenantAudience:true,secretsInMetadata:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/owners granting provider authority/);assert.match(r.errors.join('\n'),/audiences from meaning permission/);assert.match(r.errors.join('\n'),/contracts redefining semantics/);assert.match(r.errors.join('\n'),/machine owner assignment/)});test('ARCH-033-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{audienceFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===API_PRODUCT_MODEL_OWNERSHIP_AUDIENCES_ERROR_CODE);assert.throws(()=>complete(new D()).ownerFields.push('x'),TypeError)});
