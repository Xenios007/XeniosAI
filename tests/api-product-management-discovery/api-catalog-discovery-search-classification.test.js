import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_CATALOG_DISCOVERY_SEARCH_CLASSIFICATION_ERROR_CODE,ApiCatalogDiscoverySearchClassificationDescriptor as D,ApiCatalogDiscoverySearchClassificationProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['catalogFields','taxonomyFields','searchFields','filterFields','recommendationFields','visibilityFields','machineDiscoveryFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API discovery',...x})};test('ARCH-033-03 exposes catalog taxonomy search filters recommendations visibility and machine discovery',()=>{const d=new D();assert.ok(d.catalogFields().includes('indexed-at-source-updated-at-stale-at-and-status'));assert.ok(d.taxonomyFields().includes('equivalent-related-replacement-and-external-term-mappings'));assert.ok(d.searchFields().includes('stable-entry-reference-score-and-match-reasons'));assert.ok(d.filterFields().includes('source-freshness-and-policy-aware-facet-counts'));assert.ok(d.recommendationFields().includes('human-readable-match-reasons-and-source-signals'));assert.ok(d.visibilityFields().includes('visible-redacted-summarized-and-hidden-field-policy'));assert.ok(d.machineDiscoveryFields().includes('arch-034-capability-and-version-reference'))});test('ARCH-033-03 validates authority truth semantics permission recommendation registry query machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({discoveryGrantsAuthority:true,indexMeansTruth:true,taxonomyRedefinesSemantics:true,visibilityMeansPermission:true,recommendationApprovesAccess:true,unregisteredMachineContract:true,sharedTenantIndex:true,secretsInQuery:true,machineSelfPublishes:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/discovery granting authority/);assert.match(r.errors.join('\n'),/indexes from meaning provider truth/);assert.match(r.errors.join('\n'),/recommendations approving access/);assert.match(r.errors.join('\n'),/unregistered machine contracts/)});test('ARCH-033-03 detects incomplete metadata and preserves immutability',()=>{class I extends D{searchFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===API_CATALOG_DISCOVERY_SEARCH_CLASSIFICATION_ERROR_CODE);assert.throws(()=>complete(new D()).taxonomyFields.push('x'),TypeError)});
