import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_PRODUCT_LIFECYCLE_VERSIONING_DEPRECATION_ERROR_CODE,ApiProductLifecycleVersioningDeprecationDescriptor as D,ApiProductLifecycleVersioningDeprecationProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['introductionFields','previewFields','availabilityFields','versionMappingFields','deprecationFields','retirementFields','exitFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API lifecycle',...x})};test('ARCH-033-08 exposes introduction preview availability version mapping deprecation retirement and exit',()=>{const d=new D();assert.ok(d.introductionFields().includes('registered-arch-017-and-arch-034-contract-versions'));assert.ok(d.previewFields().includes('known-gaps-unsupported-use-and-non-production-constraints'));assert.ok(d.availabilityFields().includes('independent-readiness-assurance-and-release-approval'));assert.ok(d.versionMappingFields().includes('provider-service-api-and-effective-version-mappings'));assert.ok(d.deprecationFields().includes('catalog-registry-portal-documentation-sdk-and-status-visibility'));assert.ok(d.retirementFields().includes('deprecation-window-consumer-migration-data-retention-and-legal-checks'));assert.ok(d.exitFields().includes('credential-session-key-webhook-and-integration-revocation'))});test('ARCH-033-08 validates authority semantics preview deprecation retirement exit machine transition and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({lifecycleGrantsAuthority:true,versionMappingRedefinesSemantics:true,previewMeansProductionReady:true,silentDeprecation:true,retirementSkipsMigration:true,exitLeavesCredentials:true,machineSelfPromotes:true,sharedTenantMigration:true,irreversibleWithoutDisclosure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/lifecycle state granting authority/);assert.match(r.errors.join('\n'),/preview from meaning production ready/);assert.match(r.errors.join('\n'),/silent deprecation/);assert.match(r.errors.join('\n'),/exit leaving credentials/)});test('ARCH-033-08 detects incomplete metadata and preserves immutability',()=>{class I extends D{deprecationFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===API_PRODUCT_LIFECYCLE_VERSIONING_DEPRECATION_ERROR_CODE);assert.throws(()=>complete(new D()).versionMappingFields.push('x'),TypeError)});
