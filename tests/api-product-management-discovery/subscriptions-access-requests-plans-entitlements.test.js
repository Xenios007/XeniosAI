import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{SUBSCRIPTIONS_ACCESS_REQUESTS_PLANS_ENTITLEMENTS_ERROR_CODE,SubscriptionsAccessRequestsPlansEntitlementsDescriptor as D,SubscriptionsAccessRequestsPlansEntitlementsProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['subscriptionFields','accessRequestFields','approvalFields','planFields','entitlementFields','environmentFields','termsFields','revocationFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API subscriptions',...x})};test('ARCH-033-04 exposes subscriptions requests approvals plans entitlements environments terms and revocation',()=>{const d=new D();assert.ok(d.subscriptionFields().includes('requested-review-approved-provisioning-active-restricted-suspended-expired-revoked-or-closed'));assert.ok(d.accessRequestFields().includes('requested-operations-data-resources-and-limits'));assert.ok(d.approvalFields().includes('requester-reviewer-provider-and-high-risk-separation-of-duties'));assert.ok(d.planFields().includes('quota-rate-concurrency-payload-and-retention-references'));assert.ok(d.entitlementFields().includes('authoritative-provider-grant-reference'));assert.ok(d.environmentFields().includes('tenant-project-application-and-environment-isolation'));assert.ok(d.termsFields().includes('actor-authority-notice-consent-and-accepted-at'));assert.ok(d.revocationFields().includes('deny-disable-suspend-terminate-rotate-purge-and-notify'))});test('ARCH-033-04 validates authority approval entitlement environment terms revocation machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({subscriptionGrantsAuthority:true,approvalMeansRuntimePermission:true,entitlementSelfAuthoritative:true,environmentAccessTransfers:true,termsOverridePolicy:true,revokedStillOperates:true,machineSelfApproves:true,sharedTenantEntitlement:true,secretsInRequests:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/subscriptions granting authority/);assert.match(r.errors.join('\n'),/approval from meaning runtime permission/);assert.match(r.errors.join('\n'),/cross-environment access transfer/);assert.match(r.errors.join('\n'),/revoked access remaining operational/)});test('ARCH-033-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{entitlementFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===SUBSCRIPTIONS_ACCESS_REQUESTS_PLANS_ENTITLEMENTS_ERROR_CODE);assert.throws(()=>complete(new D()).planFields.push('x'),TypeError)});
