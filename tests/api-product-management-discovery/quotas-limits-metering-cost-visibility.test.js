import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{QUOTAS_LIMITS_METERING_COST_VISIBILITY_ERROR_CODE,QuotasLimitsMeteringCostVisibilityDescriptor as D,QuotasLimitsMeteringCostVisibilityProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['quotaFields','rateLimitFields','concurrencyFields','measurementFields','costReferenceFields','warningFields','appealFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API usage visibility',...x})};test('ARCH-033-05 exposes quota rate concurrency measurement cost warning and appeal contracts',()=>{const d=new D();assert.ok(d.quotaFields().includes('authoritative-provider-policy-and-version-reference'));assert.ok(d.rateLimitFields().includes('standard-limit-remaining-reset-and-retry-signals'));assert.ok(d.concurrencyFields().includes('admission-start-completion-timeout-cancel-and-release-semantics'));assert.ok(d.measurementFields().includes('complete-estimated-corrected-late-or-disputed-status'));assert.ok(d.costReferenceFields().includes('non-invoice-estimate-tax-adjustment-and-finalization-status'));assert.ok(d.warningFields().includes('portal-api-webhook-notification-and-accessible-alternative'));assert.ok(d.appealFields().includes('authorized-independent-reviewer-and-separation-of-duties'))});test('ARCH-033-05 validates enforcement quality invoice appeal machine estimate exemption and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({displayOverridesEnforcement:true,measurementMeansTruthWithoutQuality:true,costReferenceMeansInvoice:true,appealImmediatelyChangesLimit:true,machineSelfIncreases:true,sharedTenantMeter:true,secretsInDimensions:true,hiddenEstimate:true,unboundedExemption:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/display overriding enforcement/);assert.match(r.errors.join('\n'),/cost references from meaning invoices/);assert.match(r.errors.join('\n'),/appeals immediately changing limits/);assert.match(r.errors.join('\n'),/unbounded exemptions/)});test('ARCH-033-05 detects incomplete metadata and preserves immutability',()=>{class I extends D{measurementFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===QUOTAS_LIMITS_METERING_COST_VISIBILITY_ERROR_CODE);assert.throws(()=>complete(new D()).quotaFields.push('x'),TypeError)});
