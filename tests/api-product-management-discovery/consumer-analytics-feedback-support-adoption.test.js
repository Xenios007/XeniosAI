import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CONSUMER_ANALYTICS_FEEDBACK_SUPPORT_ADOPTION_ERROR_CODE,ConsumerAnalyticsFeedbackSupportAdoptionDescriptor as D,ConsumerAnalyticsFeedbackSupportAdoptionProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['funnelFields','successMeasureFields','feedbackFields','supportFields','healthFields','impactFields','analyticsFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API adoption',...x})};test('ARCH-033-07 exposes funnels success measures feedback support health impact and privacy-safe analytics',()=>{const d=new D();assert.ok(d.funnelFields().includes('discover-evaluate-request-subscribe-provision-first-call-integrate-operate-upgrade-and-exit'));assert.ok(d.successMeasureFields().includes('canonical-numerator-denominator-unit-and-calculation'));assert.ok(d.feedbackFields().includes('notice-consent-classification-minimization-retention-and-access'));assert.ok(d.supportFields().includes('time-bounded-audited-support-access-and-consent'));assert.ok(d.healthFields().includes('healthy-degraded-partial-restricted-unavailable-or-unknown'));assert.ok(d.impactFields().includes('provider-status-telemetry-support-feedback-and-confirmation-references'));assert.ok(d.analyticsFields().includes('minimum-required-fields-and-prohibited-payloads'))});test('ARCH-033-07 validates authority truth privacy support feedback health machine retention and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({analyticsChangesAuthority:true,healthMeansProviderTruth:true,telemetryCopiesProtectedData:true,supportBypassesPolicy:true,feedbackChangesPermissions:true,healthHidesUnknown:true,machineClosesCases:true,sharedTenantAnalytics:true,unboundedRetention:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/analytics changing authority/);assert.match(r.errors.join('\n'),/telemetry copying protected data/);assert.match(r.errors.join('\n'),/feedback changing permissions/);assert.match(r.errors.join('\n'),/machine case closure/)});test('ARCH-033-07 detects incomplete metadata and preserves immutability',()=>{class I extends D{analyticsFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CONSUMER_ANALYTICS_FEEDBACK_SUPPORT_ADOPTION_ERROR_CODE);assert.throws(()=>complete(new D()).healthFields.push('x'),TypeError)});
