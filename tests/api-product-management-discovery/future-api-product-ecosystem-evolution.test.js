import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_ECOSYSTEM_EVOLUTION_DIRECTIONS,FUTURE_API_PRODUCT_ECOSYSTEM_EVOLUTION_ERROR_CODE,FutureApiProductEcosystemEvolutionDescriptor as D,FutureApiProductEcosystemEvolutionProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['evolutionDirections','compositionFields','federationFields','outcomeFields','agentDiscoveryFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia future API ecosystem',...x})};test('ARCH-033-10 exposes composable federated outcome-oriented and agent-discoverable evolution',()=>{const d=new D();assert.ok(d.evolutionDirections().includes(API_ECOSYSTEM_EVOLUTION_DIRECTIONS.COMPOSABLE));assert.ok(d.evolutionDirections().includes(API_ECOSYSTEM_EVOLUTION_DIRECTIONS.FEDERATED));assert.ok(d.compositionFields().includes('partial-failure-compensation-rollback-and-support-paths'));assert.ok(d.federationFields().includes('conflict-detection-source-precedence-review-and-appeal'));assert.ok(d.outcomeFields().includes('declared-contribution-assumptions-limitations-and-alternatives'));assert.ok(d.agentDiscoveryFields().includes('declared-state-change-idempotency-compensation-and-confirmation'))});test('ARCH-033-10 validates authority semantics machine credential failure and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({compositionGrantsAuthority:true,federationOverridesProvider:true,outcomeRedefinesSemantics:true,agentSelfApprovesAccess:true,machineSelfPublishes:true,secretsInArtifacts:true,sharedTenantMetadata:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/composition granting authority/);assert.match(r.errors.join('\n'),/federation overriding providers/);assert.match(r.errors.join('\n'),/agent self-approved access/);assert.match(r.errors.join('\n'),/hidden partial failure/)});test('ARCH-033-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{agentDiscoveryFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_API_PRODUCT_ECOSYSTEM_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).federationFields.push('x'),TypeError)});
