import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_PRODUCT_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,ApiProductOperationsGovernanceAssuranceDescriptor as D,ApiProductOperationsGovernanceAssuranceProfile as P}from'../../src/api-product-management-discovery/index.js';const K=['councilFields','serviceObjectiveFields','incidentFields','policyFields','evidenceFields','reviewFields','conformanceFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia API product operations',...x})};test('ARCH-033-09 exposes councils service objectives incidents policy evidence reviews and conformance',()=>{const d=new D();assert.ok(d.councilFields().includes('proposer-reviewer-approver-provider-and-assurance-separation-of-duties'));assert.ok(d.serviceObjectiveFields().includes('objective-warning-failure-and-error-budget'));assert.ok(d.incidentFields().includes('integrity-protected-incident-decision-action-recovery-and-correction-evidence'));assert.ok(d.policyFields().includes('authoritative-policy-service-and-version'));assert.ok(d.evidenceFields().includes('digest-signature-attestation-timestamp-and-custody'));assert.ok(d.reviewFields().includes('accountable-review-owner-and-independent-reviewer'));assert.ok(d.conformanceFields().includes('independent-release-or-operation-readiness-decision'))});test('ARCH-033-09 validates authority provider incident evidence review expiry machine secret and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({governanceGrantsAuthority:true,objectivesOverrideProvider:true,incidentHidesFailure:true,mutableEvidence:true,selfApprovedReview:true,expiredEvidenceValid:true,machineApprovesException:true,sharedTenantEvidence:true,secretsInEvidence:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/governance granting authority/);assert.match(r.errors.join('\n'),/objectives overriding providers/);assert.match(r.errors.join('\n'),/self-approved reviews/);assert.match(r.errors.join('\n'),/machine-approved exceptions/)});test('ARCH-033-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{conformanceFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===API_PRODUCT_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);assert.throws(()=>complete(new D()).evidenceFields.push('x'),TypeError)});
