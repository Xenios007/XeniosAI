import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CONTRACT_SCHEMA_CAPABILITY_REGISTRY_OVERVIEW_ERROR_CODE,ContractSchemaCapabilityRegistryOverviewDescriptor as D,ContractSchemaCapabilityRegistryOverviewProfile as P,REGISTRY_ARTIFACT_TYPES}from'../../src/contract-schema-capability-registry/index.js';const K=['artifactTypes','recordFields','consumers','boundaries','qualityAttributes','capabilities','contextFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI contract and capability registry',...x})};test('ARCH-034-01 exposes registry artifacts records consumers boundaries and capabilities',()=>{const d=new D();assert.ok(d.artifactTypes().includes(REGISTRY_ARTIFACT_TYPES.API));assert.ok(d.artifactTypes().includes(REGISTRY_ARTIFACT_TYPES.AGENT));assert.ok(d.recordFields().includes('authoritative-source-reference-and-digest'));assert.ok(d.consumers().includes('policy-bounded-agents'));assert.ok(d.boundaries().includes('runtime-gateways-and-providers-enforce-execution-policy'));assert.ok(d.capabilities().includes('source-dependency-lineage-and-impact-analysis'));assert.ok(d.contextFields().includes('correlation-identifier'))});test('ARCH-034-01 validates authority semantics permission secret machine failure and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({registryGrantsAuthority:true,registryOwnsBusinessState:true,recordRedefinesSemantics:true,visibilityMeansPermission:true,directProviderDatabaseAccess:true,secretsInRecords:true,sharedTenantRecords:true,machineSelfPublishes:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/records granting authority/);assert.match(r.errors.join('\n'),/records redefining provider semantics/);assert.match(r.errors.join('\n'),/visibility from meaning permission/);assert.match(r.errors.join('\n'),/hidden partial failure/)});test('ARCH-034-01 detects incomplete metadata and preserves immutability',()=>{class I extends D{recordFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CONTRACT_SCHEMA_CAPABILITY_REGISTRY_OVERVIEW_ERROR_CODE);assert.throws(()=>complete(new D()).artifactTypes.push('x'),TypeError)});
