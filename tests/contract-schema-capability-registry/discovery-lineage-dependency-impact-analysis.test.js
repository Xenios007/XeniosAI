import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{DISCOVERY_LINEAGE_DEPENDENCY_IMPACT_ANALYSIS_ERROR_CODE,DiscoveryLineageDependencyImpactAnalysisDescriptor as D,DiscoveryLineageDependencyImpactAnalysisProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['discoveryFields','nodeFields','edgeFields','lineageFields','producerConsumerFields','impactFields','changePlanFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI registry graph',...x})};test('ARCH-034-06 exposes discovery graph lineage producer consumer impact and change-plan metadata',()=>{const d=new D();assert.ok(d.discoveryFields().includes('matched-fields-terms-taxonomy-relations-and-ranking-signals'));assert.ok(d.nodeFields().includes('authoritative-source-provenance-and-freshness'));assert.ok(d.edgeFields().includes('confirmed-declared-inferred-conflicted-or-unknown'));assert.ok(d.lineageFields().includes('missing-partial-conflicted-unavailable-and-remediation'));assert.ok(d.producerConsumerFields().includes('declaration-subscription-telemetry-validation-and-confirmation'));assert.ok(d.impactFields().includes('unregistered-consumer-stale-edge-missing-lineage-and-unknown-impact'));assert.ok(d.changePlanFields().includes('dependency-ordered-build-test-publish-migrate-and-retire-steps'))});test('ARCH-034-06 validates authority truth confidence unknown tenant secret machine freshness and traversal boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({discoveryGrantsAuthority:true,indexMeansTruth:true,inferredEdgeConfirmed:true,unknownMeansNoImpact:true,crossTenantTraversal:true,secretIndexed:true,machineApprovesChange:true,hiddenStaleness:true,unboundedTraversal:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/discovery granting authority/);assert.match(r.errors.join('\n'),/indexes from meaning provider truth/);assert.match(r.errors.join('\n'),/unknown from meaning no impact/);assert.match(r.errors.join('\n'),/unbounded traversal/)});test('ARCH-034-06 detects incomplete metadata and preserves immutability',()=>{class I extends D{impactFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===DISCOVERY_LINEAGE_DEPENDENCY_IMPACT_ANALYSIS_ERROR_CODE);assert.throws(()=>complete(new D()).edgeFields.push('x'),TypeError)});
