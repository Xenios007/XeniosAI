import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{REGISTRY_SECURITY_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE,RegistrySecurityOperationsGovernanceAssuranceDescriptor as D,RegistrySecurityOperationsGovernanceAssuranceProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['accessControlFields','integritySigningFields','serviceObjectiveFields','incidentManagementFields','operationalRecoveryFields','stewardshipFields','auditFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI registry security operations',...x})};test('ARCH-034-09 exposes access integrity objective incident recovery stewardship and audit metadata',()=>{const d=new D();assert.ok(d.accessControlFields().includes('just-in-time-break-glass-approval-monitoring-and-expiry'));assert.ok(d.integritySigningFields().includes('mandatory-verification-point-outcome-and-failure-handling'));assert.ok(d.serviceObjectiveFields().includes('error-budget-allocation-burn-rate-and-exhaustion-policy'));assert.ok(d.incidentManagementFields().includes('blameless-root-cause-contributing-factor-and-corrective-action'));assert.ok(d.operationalRecoveryFields().includes('recovery-point-and-recovery-time-objective-per-tier'));assert.ok(d.stewardshipFields().includes('dispute-conflict-and-non-compliance-escalation-path'));assert.ok(d.auditFields().includes('append-only-tamper-evident-and-signed-audit-entry'));assert.ok(d.invariants().includes('security-and-administration-audit-records-are-append-only-and-never-silently-mutated-or-purged'))});test('ARCH-034-09 validates access integrity elevation objective incident tenant secret and audit boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({registryRecordGrantsAccess:true,unsignedArtifactTrusted:true,supportAccessUnaudited:true,machineApprovesAccess:true,crossTenantAdministration:true,secretInAuditLog:true,incidentSilentlyClosed:true,slaBreachHidden:true,auditLogMutable:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/registry record from being treated as an access grant/);assert.match(r.errors.join('\n'),/trusting unsigned or unverified trust-critical artifacts/);assert.match(r.errors.join('\n'),/hidden or unreported service-objective breaches/);assert.match(r.errors.join('\n'),/mutable or silently purged audit records/);const m=d.validateProfile({profileName:'x',accessLeastPrivilege:false,artifactsIntegrityProtected:false,serviceObjectivesDefined:false,incidentsGoverned:false,recoveryTested:false,auditImmutable:false});assert.match(m.errors.join('\n'),/requires default-deny least-privilege access/);assert.match(m.errors.join('\n'),/requires an append-only retained and reviewable audit trail/)});test('ARCH-034-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{auditFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===REGISTRY_SECURITY_OPERATIONS_GOVERNANCE_ASSURANCE_ERROR_CODE);assert.throws(()=>complete(new D()).accessControlFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
