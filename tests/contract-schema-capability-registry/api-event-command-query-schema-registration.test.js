import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{API_EVENT_COMMAND_QUERY_SCHEMA_REGISTRATION_ERROR_CODE,ApiEventCommandQuerySchemaRegistrationDescriptor as D,ApiEventCommandQuerySchemaRegistrationProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['registrationFields','apiFields','eventFields','commandFields','queryFields','schemaFields','referenceFields','compatibilityFields','exampleFields','generatedArtifactFields','publicationFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI contract registration',...x})};test('ARCH-034-03 exposes API event command query schema reference compatibility example artifact and publication metadata',()=>{const d=new D();assert.ok(d.apiFields().includes('arch-017-api-product-and-version-reference'));assert.ok(d.eventFields().includes('fact-meaning-subject-time-and-ordering-semantics'));assert.ok(d.commandFields().includes('declared-events-state-effects-compensation-and-confirmation'));assert.ok(d.queryFields().includes('result-schema-source-freshness-consistency-and-completeness'));assert.ok(d.schemaFields().includes('canonical-versioned-import-and-reference-closure'));assert.ok(d.referenceFields().includes('resolved-version-source-digest-and-time'));assert.ok(d.compatibilityFields().includes('syntax-semantics-behavior-data-security-tenant-and-operational-scope'));assert.ok(d.exampleFields().includes('no-secrets-live-credentials-or-unnecessary-personal-business-data'));assert.ok(d.generatedArtifactFields().includes('complete-source-generator-build-review-and-publication-lineage'));assert.ok(d.publicationFields().includes('ownership-source-validation-compatibility-security-privacy-documentation-and-conformance'))});test('ARCH-034-03 validates authority semantics version reference example artifact machine tenant and failure boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({registrationGrantsAuthority:true,recordRedefinesSemantics:true,publishedVersionMutated:true,unresolvedReferencePublished:true,exampleContainsSecret:true,generatedArtifactUntraceable:true,machineSelfPublishes:true,sharedTenantArtifact:true,hiddenValidationFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/registration granting authority/);assert.match(r.errors.join('\n'),/published version mutation/);assert.match(r.errors.join('\n'),/secrets in examples/);assert.match(r.errors.join('\n'),/hidden validation failure/)});test('ARCH-034-03 detects incomplete metadata and preserves immutability',()=>{class I extends D{schemaFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===API_EVENT_COMMAND_QUERY_SCHEMA_REGISTRATION_ERROR_CODE);assert.throws(()=>complete(new D()).referenceFields.push('x'),TypeError)});
