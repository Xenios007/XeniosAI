import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{TOOL_AGENT_WORKFLOW_CAPABILITY_REGISTRATION_ERROR_CODE,ToolAgentWorkflowCapabilityRegistrationDescriptor as D,ToolAgentWorkflowCapabilityRegistrationProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['capabilityFields','toolFields','agentFields','workflowFields','permissionFields','dependencyFields','limitFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI callable capabilities',...x})};test('ARCH-034-04 exposes capability tool agent workflow permission dependency and limit metadata',()=>{const d=new D();assert.ok(d.capabilityFields().includes('actor-delegation-tenant-environment-purpose-and-policy-requirements'));assert.ok(d.toolFields().includes('declared-state-external-financial-privacy-and-irreversibility-effects'));assert.ok(d.agentFields().includes('declared-autonomy-level-and-promotion-demotion-criteria'));assert.ok(d.workflowFields().includes('versioned-task-tool-service-agent-human-and-subworkflow-references'));assert.ok(d.permissionFields().includes('authoritative-policy-service-and-version'));assert.ok(d.dependencyFields().includes('replacement-portability-migration-and-provider-exit'));assert.ok(d.limitFields().includes('token-compute-storage-network-and-financial-budget'))});test('ARCH-034-04 validates authority effect autonomy state permission bound machine tenant secret and failure boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({registrationGrantsAuthority:true,toolHidesEffects:true,agentExceedsAutonomy:true,workflowOwnsDomainState:true,permissionSelfAuthoritative:true,unboundedCapability:true,machineSelfRegisters:true,sharedTenantCapability:true,secretsInRecords:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/registration granting authority/);assert.match(r.errors.join('\n'),/tools hiding effects/);assert.match(r.errors.join('\n'),/agents exceeding autonomy/);assert.match(r.errors.join('\n'),/unbounded capabilities/)});test('ARCH-034-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{agentFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===TOOL_AGENT_WORKFLOW_CAPABILITY_REGISTRATION_ERROR_CODE);assert.throws(()=>complete(new D()).permissionFields.push('x'),TypeError)});
