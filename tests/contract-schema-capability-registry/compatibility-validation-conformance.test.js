import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{COMPATIBILITY_VALIDATION_CONFORMANCE_ERROR_CODE,CompatibilityValidationConformanceDescriptor as D,CompatibilityValidationConformanceProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['structuralCheckFields','semanticCheckFields','policyRuleFields','testEvidenceFields','certificationFields','exceptionFields','qualityGateFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI registry conformance',...x})};test('ARCH-034-07 exposes structural semantic policy evidence certification exception and quality-gate metadata',()=>{const d=new D();assert.ok(d.structuralCheckFields().includes('resolvable-cross-artifact-dependency-and-fragment-reference-validation'));assert.ok(d.semanticCheckFields().includes('breaking-non-breaking-conditional-or-consumer-dependent-classification'));assert.ok(d.policyRuleFields().includes('blocking-required-recommended-or-informational-severity'));assert.ok(d.testEvidenceFields().includes('minimum-necessary-secret-free-and-classification-labelled-content'));assert.ok(d.certificationFields().includes('self-declared-registry-validated-independently-reviewed-or-certified-level'));assert.ok(d.exceptionFields().includes('compensating-control-monitoring-and-follow-up-commitment'));assert.ok(d.qualityGateFields().includes('authorized-accountable-time-bound-and-audited-override-only'));assert.ok(d.invariants().includes('a-green-check-or-certificate-never-masquerades-as-a-guarantee-of-runtime-behaviour-or-provider-state'))});test('ARCH-034-07 validates authority baseline evidence unknown exception tenant secret and gate boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({selfCertificationTrusted:true,passMeansCompatible:true,waiverMeansConformant:true,machineApprovesCertification:true,crossTenantEvidence:true,secretInEvidence:true,expiredCertificationValid:true,gateBypassSilent:true,unknownMeansConformant:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/unverified self-certification as truth/);assert.match(r.errors.join('\n'),/passing check from meaning guaranteed runtime compatibility/);assert.match(r.errors.join('\n'),/expired certification from remaining valid/);assert.match(r.errors.join('\n'),/silent quality-gate bypass/);assert.match(r.errors.join('\n'),/unknown or skipped checks from meaning conformant/);const m=d.validateProfile({profileName:'x',structurallyValidated:false,semanticallyChecked:false,policyEnforced:false,evidenceBacked:false,gatesEnforced:false,exceptionsGoverned:false});assert.match(m.errors.join('\n'),/requires structural validation/);assert.match(m.errors.join('\n'),/requires enforced quality gates/)});test('ARCH-034-07 detects incomplete metadata and preserves immutability',()=>{class I extends D{policyRuleFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===COMPATIBILITY_VALIDATION_CONFORMANCE_ERROR_CODE);assert.throws(()=>complete(new D()).certificationFields.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
