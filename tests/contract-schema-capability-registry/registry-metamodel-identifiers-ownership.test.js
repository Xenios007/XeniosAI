import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{REGISTRY_METAMODEL_IDENTIFIERS_OWNERSHIP_ERROR_CODE,RegistryMetamodelIdentifiersOwnershipDescriptor as D,RegistryMetamodelIdentifiersOwnershipProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['metamodelFields','identifierFields','kindFields','ownerFields','namespaceFields','versionFields','classificationFields','lifecycleFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI registry metamodel',...x})};test('ARCH-034-02 exposes artifact identity kind owner namespace version classification and lifecycle',()=>{const d=new D();assert.ok(d.metamodelFields().includes('stable-globally-unambiguous-artifact-identifier'));assert.ok(d.identifierFields().includes('immutable-identifier-allocation-and-change-history'));assert.ok(d.kindFields().includes('metamodel-schema-and-version'));assert.ok(d.ownerFields().includes('explicit-scoped-time-bounded-and-revocable-delegation'));assert.ok(d.namespaceFields().includes('name-allocation-reservation-and-collision-rules'));assert.ok(d.versionFields().includes('immutable-version-identifier'));assert.ok(d.classificationFields().includes('authoritative-classification-policy-and-version'));assert.ok(d.lifecycleFields().includes('replacement-compatibility-migration-support-and-deadline'))});test('ARCH-034-02 validates identity version ownership namespace classification machine secret and lifecycle boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({identifierGrantsAuthority:true,identifierReused:true,versionMutated:true,ownerSelfAssigned:true,namespaceCrossesTenant:true,classificationMeansPermission:true,machineAcceptsOwnership:true,secretsInProvenance:true,silentLifecycleChange:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/identifiers granting authority/);assert.match(r.errors.join('\n'),/identifier reuse/);assert.match(r.errors.join('\n'),/published version mutation/);assert.match(r.errors.join('\n'),/machine ownership acceptance/)});test('ARCH-034-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{ownerFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===REGISTRY_METAMODEL_IDENTIFIERS_OWNERSHIP_ERROR_CODE);assert.throws(()=>complete(new D()).identifierFields.push('x'),TypeError)});
