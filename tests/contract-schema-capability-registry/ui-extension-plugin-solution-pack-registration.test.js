import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{UI_EXTENSION_PLUGIN_SOLUTION_PACK_REGISTRATION_ERROR_CODE,UiExtensionPluginSolutionPackRegistrationDescriptor as D,UiExtensionPluginSolutionPackRegistrationProfile as P}from'../../src/contract-schema-capability-registry/index.js';const K=['manifestFields','uiExtensionFields','pluginFields','solutionPackFields','packageFields','slotFields','trustFields','compatibilityFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI extension registry',...x})};test('ARCH-034-05 exposes manifest UI plugin solution package slot trust and compatibility metadata',()=>{const d=new D();assert.ok(d.manifestFields().includes('specific-machine-readable-capability-requests-and-purpose'));assert.ok(d.uiExtensionFields().includes('sandbox-origin-process-storage-network-and-tenant-boundaries'));assert.ok(d.pluginFields().includes('source-build-sbom-attestation-advisory-certification-and-revocation'));assert.ok(d.solutionPackFields().includes('included-bounded-contexts-owned-state-and-explicit-exclusions'));assert.ok(d.packageFields().includes('no-tenant-configuration-credentials-secrets-runtime-state-or-tenant-data'));assert.ok(d.slotFields().includes('loading-empty-error-revoked-and-unavailable-experience'));assert.ok(d.trustFields().includes('security-privacy-data-accessibility-license-and-architecture-review'));assert.ok(d.compatibilityFields().includes('adapter-upgrade-configuration-data-and-rollback-path'))});test('ARCH-034-05 validates authority immutability capability trust core tenant secret machine and revocation boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({manifestGrantsAuthority:true,packageMutated:true,undeclaredCapability:true,signatureMeansSafe:true,solutionForksCore:true,extensionCrossesTenant:true,secretsInPackage:true,machineSelfInstalls:true,revokedStillLoads:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/manifests granting authority/);assert.match(r.errors.join('\n'),/package mutation/);assert.match(r.errors.join('\n'),/undeclared capabilities/);assert.match(r.errors.join('\n'),/signatures from meaning safe behavior/)});test('ARCH-034-05 detects incomplete metadata and preserves immutability',()=>{class I extends D{packageFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===UI_EXTENSION_PLUGIN_SOLUTION_PACK_REGISTRATION_ERROR_CODE);assert.throws(()=>complete(new D()).trustFields.push('x'),TypeError)});
