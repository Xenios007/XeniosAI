import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{ADMINISTRATION_OPERATIONS_DATA_COMPONENTS_ERROR_CODE,AdministrationOperationsDataComponentsDescriptor as D,AdministrationOperationsDataComponentsProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['formFields','tableFields','filterFields','queueFields','timelineFields','approvalFields','auditViewFields','bulkActionFields','safeStateFields','controls','failureRecovery','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia administration',...x})};test('ARCH-032-05 exposes forms tables filters queues timelines approvals audit bulk actions and safe state',()=>{const d=new D();assert.ok(d.formFields().includes('idempotency-concurrency-version-and-status-query'));assert.ok(d.tableFields().includes('explicit-page-query-or-identifier-selection-scope'));assert.ok(d.filterFields().includes('canonical-shareable-state-without-secrets'));assert.ok(d.queueFields().includes('assignee-team-lease-and-expiry'));assert.ok(d.timelineFields().includes('authoritative-source-version-and-provenance'));assert.ok(d.approvalFields().includes('separation-of-duties-and-conflict-check'));assert.ok(d.auditViewFields().includes('authoritative-integrity-protected-evidence-reference'));assert.ok(d.bulkActionFields().includes('dry-run-impact-conflicts-and-policy-results'));assert.ok(d.safeStateFields().includes('explicit-partial-data-and-missing-sources'))});test('ARCH-032-05 validates authority projection bulk approval audit failure secret machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({clientStateGrantsAuthority:true,projectionMeansTruth:true,bulkSelectionMeansEligibility:true,requesterSelfApproves:true,auditEvidenceMutable:true,hiddenPartialFailure:true,sharedTenantSelection:true,secretsInFilters:true,machineExecutesSilently:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/client state granting authority/);assert.match(r.errors.join('\n'),/bulk selection from meaning eligibility/);assert.match(r.errors.join('\n'),/requester self-approval/);assert.match(r.errors.join('\n'),/hidden partial failure/)});test('ARCH-032-05 detects incomplete metadata and preserves immutability',()=>{class I extends D{bulkActionFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===ADMINISTRATION_OPERATIONS_DATA_COMPONENTS_ERROR_CODE);assert.throws(()=>complete(new D()).safeStateFields.push('x'),TypeError)});
