import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FRONTEND_SDK_CAPABILITY_API_CLIENTS_ERROR_CODE,FrontendSdkCapabilityApiClientsDescriptor as D,FrontendSdkCapabilityApiClientsProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['typedClientFields','identityContextFields','capabilityDiscoveryFields','retryFields','streamingFields','errorFields','contractVersionFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia frontend clients',...x})};test('ARCH-032-03 exposes typed clients identity discovery retries streaming errors and contract versions',()=>{const d=new D();assert.ok(d.typedClientFields().includes('generated-types-with-source-provenance'));assert.ok(d.identityContextFields().includes('short-lived-credential-reference-only'));assert.ok(d.capabilityDiscoveryFields().includes('authoritative-registry-reference'));assert.ok(d.retryFields().includes('authoritative-status-query-before-uncertain-retry'));assert.ok(d.streamingFields().includes('backpressure-buffer-and-rate-budget'));assert.ok(d.errorFields().includes('stable-error-code'));assert.ok(d.contractVersionFields().includes('registered-schema-and-provenance-reference'))});test('ARCH-032-03 validates authority identity retry stream error version machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({generatedTypeGrantsAuthority:true,discoveryMeansPermission:true,ambientCredential:true,blindCommandRetry:true,streamMeansCompletion:true,protectedErrorDetails:true,sharedTenantCache:true,unversionedClient:true,machineSelfPublishes:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/generated types granting authority/);assert.match(r.errors.join('\n'),/blind command retries/);assert.match(r.errors.join('\n'),/streams from meaning completion/);assert.match(r.errors.join('\n'),/protected error details/)});test('ARCH-032-03 detects incomplete metadata and preserves immutability',()=>{class I extends D{streamingFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FRONTEND_SDK_CAPABILITY_API_CLIENTS_ERROR_CODE);assert.throws(()=>complete(new D()).contractVersionFields.push('x'),TypeError)});
