import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{APPLICATION_SHELL_COMPOSITION_ROUTING_NAVIGATION_ERROR_CODE,ApplicationShellCompositionRoutingNavigationDescriptor as D,ApplicationShellCompositionRoutingNavigationProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['shellFields','routeFields','navigationFields','layoutFields','capabilityLoadingFields','errorBoundaryFields','compositionFields','lifecycleStates','controls','failureRecovery','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia application shell',...x})};test('ARCH-032-02 exposes shells routes navigation layouts loading boundaries composition and lifecycle',()=>{const d=new D();assert.ok(d.shellFields().includes('versioned-layout-region-contract'));assert.ok(d.routeFields().includes('canonical-route-pattern'));assert.ok(d.navigationFields().includes('keyboard-assistive-and-no-script-alternative'));assert.ok(d.capabilityLoadingFields().includes('integrity-protected-artifact-reference'));assert.ok(d.errorBoundaryFields().includes('accessible-safe-and-non-authorizing-fallback'));assert.deepEqual(d.lifecycleStates(),['discover','request','review','enabled','rejected','active','restricted','deprecated','retired'])});test('ARCH-032-02 validates authority navigation loading failure tenant extension secret and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({routeGrantsAuthority:true,navigationMeansPermission:true,clientOwnsBusinessState:true,unverifiedArtifactLoaded:true,errorHidden:true,sharedTenantState:true,extensionBypassesMediation:true,machineSelfEnables:true,secretsInUrls:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/routes granting authority/);assert.match(r.errors.join('\n'),/navigation from meaning permission/);assert.match(r.errors.join('\n'),/unverified artifact/);assert.match(r.errors.join('\n'),/hidden errors/)});test('ARCH-032-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{routeFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===APPLICATION_SHELL_COMPOSITION_ROUTING_NAVIGATION_ERROR_CODE);assert.throws(()=>complete(new D()).compositionFields.push('x'),TypeError)});
