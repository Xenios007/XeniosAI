import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{UI_EXTENSION_EMBEDDING_SECURITY_BOUNDARIES_ERROR_CODE,UiExtensionEmbeddingSecurityBoundariesDescriptor as D,UiExtensionEmbeddingSecurityBoundariesProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['slotFields','manifestFields','grantFields','isolationFields','sandboxFields','contentSecurityFields','communicationFields','revocationFields','controls','failureRecovery','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia UI extensions',...x})};test('ARCH-032-08 exposes slots manifests grants isolation sandbox content communication and revocation',()=>{const d=new D();assert.ok(d.slotFields().includes('maximum-allowlisted-capability-set'));assert.ok(d.manifestFields().includes('arch-019-package-and-version-reference'));assert.ok(d.grantFields().includes('specific-action-data-and-resource-scope'));assert.ok(d.isolationFields().includes('no-ambient-host-session-or-credential-access'));assert.ok(d.sandboxFields().includes('deny-by-default-browser-permission-policy'));assert.ok(d.contentSecurityFields().includes('trusted-types-and-safe-dom-sink-policy'));assert.ok(d.communicationFields().includes('origin-source-and-installation-validation'));assert.ok(d.revocationFields().includes('disable-terminate-disconnect-purge-and-block'))});test('ARCH-032-08 validates authority credential database artifact communication storage revocation machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({installationGrantsAuthority:true,ambientHostCredential:true,directProviderDatabaseAccess:true,unverifiedArtifact:true,wildcardCommunication:true,sharedTenantStorage:true,revokedStillRuns:true,machineSelfGrants:true,protectedPayloadInMessages:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/installation granting authority/);assert.match(r.errors.join('\n'),/ambient host credentials/);assert.match(r.errors.join('\n'),/wildcard communication/);assert.match(r.errors.join('\n'),/revoked extensions remaining operational/)});test('ARCH-032-08 detects incomplete metadata and preserves immutability',()=>{class I extends D{isolationFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===UI_EXTENSION_EMBEDDING_SECURITY_BOUNDARIES_ERROR_CODE);assert.throws(()=>complete(new D()).grantFields.push('x'),TypeError)});
