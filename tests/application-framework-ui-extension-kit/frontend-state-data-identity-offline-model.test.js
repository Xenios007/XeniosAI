import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FRONTEND_STATE_DATA_IDENTITY_OFFLINE_MODEL_ERROR_CODE,FrontendStateDataIdentityOfflineModelDescriptor as D,FrontendStateDataIdentityOfflineModelProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['localStateFields','serverStateFields','cacheFields','identityContextFields','optimisticFields','offlineFields','recoveryFields','controls','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia frontend state',...x})};test('ARCH-032-06 exposes local server cache identity optimistic offline and recovery contracts',()=>{const d=new D();assert.ok(d.localStateFields().includes('versioned-local-state-schema'));assert.ok(d.serverStateFields().includes('etag-revision-or-resource-version'));assert.ok(d.cacheFields().includes('logout-tenant-change-revocation-expiry-and-remote-purge'));assert.ok(d.identityContextFields().includes('explicit-context-switch-reset-and-reload-evidence'));assert.ok(d.optimisticFields().includes('visible-pending-and-not-yet-confirmed-state'));assert.ok(d.offlineFields().includes('device-binding-loss-revocation-and-remote-purge'));assert.ok(d.recoveryFields().includes('local-server-cache-optimistic-offline-and-provider-reconciliation'))});test('ARCH-032-06 validates authority truth optimistic offline conflict context identity and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({clientStateGrantsAuthority:true,cacheMeansTruth:true,optimisticMeansCompletion:true,offlineMeansAuthorization:true,silentConflictOverwrite:true,contextSwitchKeepsState:true,ambientCredential:true,sharedTenantCache:true,machineResolvesSilently:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/client state granting authority/);assert.match(r.errors.join('\n'),/optimistic state from meaning completion/);assert.match(r.errors.join('\n'),/silent conflict overwrite/);assert.match(r.errors.join('\n'),/shared tenant caches/)});test('ARCH-032-06 detects incomplete metadata and preserves immutability',()=>{class I extends D{offlineFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FRONTEND_STATE_DATA_IDENTITY_OFFLINE_MODEL_ERROR_CODE);assert.throws(()=>complete(new D()).cacheFields.push('x'),TypeError)});
