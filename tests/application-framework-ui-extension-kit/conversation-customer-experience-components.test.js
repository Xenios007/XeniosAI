import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{CONVERSATION_CUSTOMER_EXPERIENCE_COMPONENTS_ERROR_CODE,ConversationCustomerExperienceComponentsDescriptor as D,ConversationCustomerExperienceComponentsProfile as P}from'../../src/application-framework-ui-extension-kit/index.js';const K=['chatFields','structuredInputFields','streamingFields','citationFields','confirmationFields','handoffFields','historyFields','feedbackFields','accessibilityFields','controls','failureRecovery','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Casa Lluvia customer conversation',...x})};test('ARCH-032-04 exposes chat input streaming citations confirmation handoff history feedback and accessibility',()=>{const d=new D();assert.ok(d.chatFields().includes('attributable-human-agent-service-and-system-participants'));assert.ok(d.structuredInputFields().includes('deterministic-client-and-authoritative-provider-validation'));assert.ok(d.streamingFields().includes('accessible-buffered-or-non-streaming-alternative'));assert.ok(d.citationFields().includes('retrieval-transformation-and-generation-provenance'));assert.ok(d.confirmationFields().includes('provider-confirmed-pending-rejected-failed-or-completed-result'));assert.ok(d.handoffFields().includes('minimum-authorized-summary-and-source-references'));assert.ok(d.historyFields().includes('correction-appeal-and-supersession-reference'));assert.ok(d.feedbackFields().includes('accessible-response-appeal-and-support-path'))});test('ARCH-032-04 validates confirmation citation stream handoff history feedback secret machine and tenant boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({clientConfirmsBusinessAction:true,citationMeansPermission:true,streamMeansCompletion:true,silentContextSharing:true,uncorrectableHistory:true,feedbackChangesAuthority:true,secretsInConversation:true,machineSelfConfirms:true,tenantIsolated:false});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/client-confirmed business actions/);assert.match(r.errors.join('\n'),/citations from meaning permission/);assert.match(r.errors.join('\n'),/silent context sharing/);assert.match(r.errors.join('\n'),/machine self-confirmation/)});test('ARCH-032-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{accessibilityFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===CONVERSATION_CUSTOMER_EXPERIENCE_COMPONENTS_ERROR_CODE);assert.throws(()=>complete(new D()).confirmationFields.push('x'),TypeError)});
