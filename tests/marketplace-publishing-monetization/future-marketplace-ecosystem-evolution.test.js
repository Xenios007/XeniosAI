import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FUTURE_MARKETPLACE_ECOSYSTEM_EVOLUTION_ERROR_CODE,FutureMarketplaceEcosystemEvolutionDescriptor as D,FutureMarketplaceEcosystemEvolutionProfile as P}from'../../src/marketplace-publishing-monetization/index.js';const K=['evolutionDirections','federatedCatalogFields','portableLicenseFields','composableProductFields','sustainableEconomicsFields','controls','failureRecovery','observabilityFields','assuranceEvidence','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'XeniosAI marketplace evolution',...x})};test('ARCH-039-10 exposes evolution-direction federated-catalog portable-license composable-product and economics metadata',()=>{const d=new D();assert.ok(d.evolutionDirections().includes('portable-licenses-and-entitlements-that-move-across-marketplaces-and-hosts'));assert.ok(d.federatedCatalogFields().includes('non-authoritative-federated-listing-labelling'));assert.ok(d.portableLicenseFields().includes('the-provider-still-resolves-and-enforces-the-entitlement-at-runtime'));assert.ok(d.composableProductFields().includes('a-rolled-up-and-transparent-provenance-and-certification-view'));assert.ok(d.sustainableEconomicsFields().includes('anti-self-preferencing-and-anti-monopoly-safeguards'));assert.ok(d.invariants().includes('a-purchased-license-is-never-locked-to-a-single-marketplace-or-host'))});test('ARCH-039-10 validates authority provider-resolution provenance economics tenant and machine boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const r=d.validateProfile({federatedCatalogGrantsAuthority:true,portableLicenseBypassesProvider:true,composableProductHidesProvenance:true,economicsFavourIncumbents:true,aiSilentlyEvolvesMarketplace:true,secretsInFederatedListings:true,crossTenantFederatedState:true,hiddenPartialFailure:true});assert.equal(r.isValid,false);assert.match(r.errors.join('\n'),/a portable license bypassing provider runtime resolution and enforcement/);assert.match(r.errors.join('\n'),/a composable product hiding the provenance or certification status of a sub-product/);assert.match(r.errors.join('\n'),/ecosystem economics structurally favouring incumbents through undisclosed preferencing/);assert.match(r.errors.join('\n'),/AI assistance silently evolving the marketplace or changing its economics/);const m=d.validateProfile({profileName:'x',licensesPortable:false,economicsSustainable:false,providerAuthorityPreserved:false});assert.match(m.errors.join('\n'),/requires licenses and entitlements to remain portable and free of marketplace lock-in/);assert.match(m.errors.join('\n'),/requires fair transparent ecosystem economics that protect small-publisher viability/)});test('ARCH-039-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{portableLicenseFields(){return[]}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_MARKETPLACE_ECOSYSTEM_EVOLUTION_ERROR_CODE);assert.throws(()=>complete(new D()).evolutionDirections.push('x'),TypeError);assert.equal(new D().assertArchitecture().isValid,true)});
