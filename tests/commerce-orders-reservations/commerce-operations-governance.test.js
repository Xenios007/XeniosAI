import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{COMMERCE_OPERATIONS_GOVERNANCE_ERROR_CODE,COMMERCE_OPERATIONAL_CONTROLS,COMMERCE_GOVERNANCE_INVARIANTS,CommerceOperationsGovernanceDescriptor as D,CommerceOperationsGovernanceProfile as P}from'../../src/commerce-orders-reservations/index.js';const K=['responsibilities','serviceObjectives','operationalControls','fraudAbuseBoundaries','observabilityFields','incidentFields','recovery','assuranceEvidence','commandFields','eventFields','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Commerce Operations Governance',...x})};test('ARCH-025-09 exposes operations objectives fraud assurance and invariant metadata',()=>{const d=new D();assert.ok(d.serviceObjectives().includes('oversell-incidents'));assert.ok(d.fraudAbuseBoundaries().includes('fraud-model-output-cannot-commit-or-cancel-aggregates'));assert.ok(d.operationalControls().includes(COMMERCE_OPERATIONAL_CONTROLS.AUDIT));assert.ok(d.invariants().includes(COMMERCE_GOVERNANCE_INVARIANTS.FRAUD_NO_AUTHORITY))});test('ARCH-025-09 validates governance and fraud boundaries',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({fraudCommitsAggregate:true,fraudCancelsAggregate:true,unboundedRateOrVolume:true,auditEvidenceOmitted:true,pricingOrPaymentOwnedHere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/fraud signals from committing aggregates/);assert.match(x.errors.join('\n'),/unbounded rates or volumes/);assert.match(x.errors.join('\n'),/omitted audit evidence/)});test('ARCH-025-09 detects incomplete metadata and preserves immutability',()=>{class I extends D{serviceObjectives(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===COMMERCE_OPERATIONS_GOVERNANCE_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.serviceObjectives.push('x'),TypeError)});
