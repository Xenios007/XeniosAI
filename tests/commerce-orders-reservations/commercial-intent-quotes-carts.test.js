import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{COMMERCIAL_INTENT_QUOTES_CARTS_ERROR_CODE,CommercialIntentQuotesCartsDescriptor as D,CommercialIntentQuotesCartsProfile as P,INTENT_QUOTES_CARTS_CONTROLS,INTENT_QUOTES_CARTS_INVARIANTS}from'../../src/commerce-orders-reservations/index.js';const K=['responsibilities','lifecycleStages','quoteFields','cartFields','commandFields','eventFields','controls','recovery','observability','measures','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Intent Quotes Carts',...x})};test('ARCH-025-02 exposes intent quote cart lifecycle and conversion metadata',()=>{const d=new D();assert.ok(d.lifecycleStages().includes('quoted'));assert.ok(d.quoteFields().includes('valid-until'));assert.ok(d.controls().includes(INTENT_QUOTES_CARTS_CONTROLS.RECALCULATION));assert.ok(d.invariants().includes(INTENT_QUOTES_CARTS_INVARIANTS.VERSIONED_CONVERSION))});test('ARCH-025-02 validates quote and conversion safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({expiredQuoteConfirmed:true,generatedStatementIsConfirmation:true,unapprovedConversion:true,pricingOrPaymentOwnedHere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/expired quotes/);assert.match(x.errors.join('\n'),/generated or cached statements as confirmation/);assert.match(x.errors.join('\n'),/unapproved conversion/)});test('ARCH-025-02 detects incomplete metadata and preserves immutability',()=>{class I extends D{quoteFields(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===COMMERCIAL_INTENT_QUOTES_CARTS_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.quoteFields.push('x'),TypeError)});
