import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{COMMERCE_EVOLUTION_HORIZONS,EVOLUTION_CONTROLS,EVOLUTION_INVARIANTS,FUTURE_COMMERCE_EVOLUTION_ERROR_CODE,FutureCommerceEvolutionDescriptor as D,FutureCommerceEvolutionProfile as P}from'../../src/commerce-orders-reservations/index.js';const K=['evolutionPrinciples','evolutionHorizons','distributedAvailability','composableFulfillment','agentAssistedTransactions','researchPromotionRetirement','recordFields','controls','recovery','observability','measures','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Future Commerce Evolution',...x})};test('ARCH-025-10 exposes future horizons distributed fulfillment agent and authority metadata',()=>{const d=new D();assert.ok(d.evolutionHorizons().includes(COMMERCE_EVOLUTION_HORIZONS.AGENT_ASSISTED));assert.ok(d.agentAssistedTransactions().includes('agent-output-cannot-commit-or-cancel-aggregates'));assert.ok(d.controls().includes(EVOLUTION_CONTROLS.HUMAN_APPROVAL));assert.ok(d.invariants().includes(EVOLUTION_INVARIANTS.AGENT_NO_AUTHORITY))});test('ARCH-025-10 validates bounded commerce evolution safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({unboundedExperimentation:true,agentCommitsAggregate:true,agentCancelsAggregate:true,pricingOrPaymentOwnedHere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/unbounded experimentation/);assert.match(x.errors.join('\n'),/agents from committing aggregates/);assert.match(x.errors.join('\n'),/agents from cancelling aggregates/)});test('ARCH-025-10 detects incomplete metadata and preserves immutability',()=>{class I extends D{evolutionHorizons(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FUTURE_COMMERCE_EVOLUTION_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.controls.push('x'),TypeError)});
