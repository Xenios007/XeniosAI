import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{RESERVATION_BOOKING_LIFECYCLE_ERROR_CODE,RESERVATION_CONTROLS,RESERVATION_INVARIANTS,ReservationBookingLifecycleDescriptor as D,ReservationBookingLifecycleProfile as P}from'../../src/commerce-orders-reservations/index.js';const K=['responsibilities','states','transitions','reservationFields','participantFields','timeIntervalFields','resourceReferenceFields','commandFields','eventFields','controls','recovery','observability','measures','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Reservation Lifecycle',...x})};test('ARCH-025-04 exposes reservation time participant resource and extension metadata',()=>{const d=new D();assert.ok(d.states().includes('confirmed'));assert.ok(d.timeIntervalFields().includes('time-zone'));assert.ok(d.controls().includes(RESERVATION_CONTROLS.EXTENSION_BOUNDARY));assert.ok(d.invariants().includes(RESERVATION_INVARIANTS.AUDITABLE_COMMITMENT))});test('ARCH-025-04 validates reservation boundary safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({unvalidatedTime:true,unvalidatedParticipants:true,unvalidatedResources:true,unboundedExtensions:true,pricingOrPaymentOwnedHere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/unvalidated time semantics/);assert.match(x.errors.join('\n'),/unvalidated participants/);assert.match(x.errors.join('\n'),/unbounded industry extensions/)});test('ARCH-025-04 detects incomplete metadata and preserves immutability',()=>{class I extends D{states(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===RESERVATION_BOOKING_LIFECYCLE_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.states.push('x'),TypeError)});
