import assert from'node:assert/strict';import test from'node:test';import{PlatformError}from'../../src/foundation/index.js';import{FULFILLMENT_ADDONS_PACKAGES_ERROR_CODE,FULFILLMENT_CONTROLS,FULFILLMENT_INVARIANTS,FulfillmentAddOnsPackagesDescriptor as D,FulfillmentAddOnsPackagesProfile as P}from'../../src/commerce-orders-reservations/index.js';const K=['responsibilities','states','transitions','fulfillmentPlanFields','componentFields','addOnFields','substitutionFields','exceptionFields','commandFields','eventFields','controls','recovery','observability','measures','invariants'];const complete=d=>{const x={};for(const k of K)x[k]=d[k]();return new P({profileName:'Fulfillment Add-ons Packages',...x})};test('ARCH-025-07 exposes fulfillment components add-ons substitutions and evidence metadata',()=>{const d=new D();assert.ok(d.states().includes('partial'));assert.ok(d.componentFields().includes('dependency-references'));assert.ok(d.controls().includes(FULFILLMENT_CONTROLS.SUBSTITUTION_APPROVAL));assert.ok(d.invariants().includes(FULFILLMENT_INVARIANTS.PARTIAL_EXPLICIT))});test('ARCH-025-07 validates fulfillment boundary safeguards',()=>{const d=new D();assert.equal(d.validateProfile(complete(d)).isValid,true);const x=d.validateProfile({unvalidatedDependencies:true,unapprovedSubstitution:true,partialCompletionHidden:true,pricingOrPaymentOwnedHere:true});assert.equal(x.isValid,false);assert.match(x.errors.join('\n'),/unvalidated dependencies/);assert.match(x.errors.join('\n'),/unapproved substitutions/);assert.match(x.errors.join('\n'),/hidden partial completion/)});test('ARCH-025-07 detects incomplete metadata and preserves immutability',()=>{class I extends D{states(){return []}}assert.throws(()=>new I().assertArchitecture(),e=>e instanceof PlatformError&&e.code===FULFILLMENT_ADDONS_PACKAGES_ERROR_CODE);const p=complete(new D());assert.ok(Object.isFrozen(p));assert.throws(()=>p.states.push('x'),TypeError)});
